# Phase 4.5 Verification

**Date:** 2026-07-03
**Scope:** local Isaac-free verification plus server Isaac stub smoke

## Local Tests

```powershell
python -m pytest -q tests\test_policy_adapter.py tests\test_ppo_koopman_logging.py tests\test_phase45_source_contract.py tests\test_phase45_checkpoint_discovery.py
```

Result:

```text
22 passed
```

```powershell
python -m pytest -q
```

Result:

```text
92 passed
```

## Compile Check

```powershell
python -m compileall __init__.py easyuuv_env.py koopman workflows tests
```

Result:

```text
passed
```

## Diff Hygiene

```powershell
git diff --check
```

Result:

```text
passed
```

## Server Verification

Code was synced to `agentic-AUV` with a `git archive` tarball from local commit `97025ed`.

Remote Python checks:

```bash
cd /root/EASYkoopman
/opt/conda/envs/isaaclab/bin/python -m pytest -q
/opt/conda/envs/isaaclab/bin/python -m compileall __init__.py easyuuv_env.py koopman workflows tests
```

Result:

```text
92 passed
compileall passed
```

Isaac stub smoke:

```bash
cd /root/IsaacLab
WANDB_MODE=disabled ./isaaclab.sh -p /root/EASYkoopman/workflows/play_ppo_koopman.py \
  --task EasyUUV-Direct-v1 \
  --num_envs 1 \
  --headless \
  --policy_mode stub \
  --controller_mode koopman_mpc \
  --koopman_manifest_path /root/EASYkoopman/source/results/koopman_phase2_5_verify_20260701_231802/selected_model_manifest.json \
  --trajectory_type step \
  --trajectory_cycles 1 \
  --steps_per_action 50 \
  --ppo_koopman_log_path /root/EASYkoopman/source/results/koopman_phase4_5/stub_step_97025ed.jsonl
```

Validation:

```bash
cd /root/EASYkoopman
/opt/conda/envs/isaaclab/bin/python workflows/validate_ppo_koopman_log.py \
  source/results/koopman_phase4_5/stub_step_97025ed.jsonl
```

Result:

```text
OK: 350 PPO/Koopman samples
adapter_modes=heuristic_reference_delta_v0
ppo_evidence_levels=stub_only
policy_modes=stub
backend_used=direct_state
controller_modes=koopman_mpc/Ssurface
```

Metric summary:

```text
samples: 350
fallback_count: 98
fallback_rate: 0.28
latency_ms_mean: 11.87197584392769
latency_ms_max: 12.386091984808445
policy_action_clip_rate_max: 0.0
base_reference_goal_match_max_error_max: 0.0
pwm_min: -1.0
pwm_max: 1.0
```

Checkpoint discovery:

```bash
cd /root/EASYkoopman
/opt/conda/envs/isaaclab/bin/python workflows/discover_ppo_checkpoints.py --json
```

Result:

```text
checkpoint_found=false
count=0
```

Checkpoint missing preflight:

```text
CHECKPOINT_MISSING_EXIT_CODE=2
CHECKPOINT_MISSING_SECONDS=0
No PPO checkpoint found before Isaac app startup.
```

Checkpoint smoke was not run because no PPO checkpoint exists on the server.
