---
phase: 07-cross-configuration-koopman-data-and-control-contract
plan: "03"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - koopman/dataset_v2.py
  - koopman/v1_compatibility.py
  - tests/fixtures/koopman_v2_three_topologies/
  - tests/test_koopman_dataset_v2.py
  - tests/test_koopman_v1_compatibility.py
autonomous: true
requirements: [CONT-02, CONT-03, CONT-05]
decisions: [D-01, D-02, D-03, D-04, D-06, D-09, D-10, D-12, D-16]
user_setup: []
must_haves:
  truths:
    - "D-01/D-04: a separately named v2 dataset exposes U with exact shape (n,4) from virtual_control_4 while the v1 KoopmanDataset remains U=(n,8)"
    - "D-06: padded PWM, mask and wrench live only in a named diagnostics view and cannot silently enter default v2 training control"
    - "D-02/D-03: v1 logs are read only through an explicit compatibility view that cannot become a schema-v2 record or eligible cross-configuration dataset"
    - "D-09/D-12: context and provenance remain attached to v2 samples; local fixtures stay local_contract"
  artifacts:
    - path: koopman/dataset_v2.py
      provides: immutable named model-facing U=virtual_control_4 dataset plus actuator diagnostics
      contains: "class KoopmanDatasetV2"
    - path: koopman/v1_compatibility.py
      provides: explicit read-only v1 compatibility result with unavailable fields and false training eligibility
      contains: "class V1CompatibilityView"
    - path: tests/test_koopman_dataset_v2.py
      provides: 4D-control, diagnostics separation, topology and context-retention tests
      contains: "test_dataset_v2_u_is_virtual_control_4"
    - path: tests/test_koopman_v1_compatibility.py
      provides: canonical v1 read/non-promotion and unchanged-v1 regression tests
      contains: "test_v1_view_is_never_v2_training_eligible"
  key_links:
    - from: koopman/dataset_v2.py
      to: koopman/schema_v2.py
      via: strict episode loader and named `virtual_control_4` extraction
      pattern: "load_koopman_episode_v2"
    - from: koopman/v1_compatibility.py
      to: koopman_data.py
      via: existing v1 loader without field promotion or inference
      pattern: "load_koopman_samples"
---

<objective>
建立安全的 v2 model-facing 数据视图和明确的 v1 只读兼容入口，使 Phase 8 可以获得统一 4D control，同时让 PWM diagnostics、context 与历史 v1 证据边界保持可审计。

Purpose: 防止最危险的静默漂移——虽然文件名叫 v2，但 `.U` 仍读取 padded PWM，或把缺字段的 v1 日志补成伪 v2 数据。
Output: `KoopmanDatasetV2`、`ActuatorDiagnostics`、`V1CompatibilityView`、三拓扑 fixture 和双向隔离测试。
</objective>

<execution_context>
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-SPEC.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-CONTEXT.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-RESEARCH.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-PATTERNS.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-01-SUMMARY.md
</execution_context>

<context>
@koopman/schema_v2.py
@koopman/dataset.py
@koopman_data.py
@koopman/model.py
@koopman/lifted_edmd.py
@koopman/mpc.py
@tests/test_koopman_dataset.py
@tests/test_koopman_data.py
@tests/fixtures/koopman_step_small.jsonl
</context>

<threat_model>
  <assets>
    <asset>Meaning and dimensionality of model-facing control `U`</asset>
    <asset>Frozen v1 logs, loader, dataset, model and MPC behavior</asset>
    <asset>Topology/context/provenance needed for later cross-configuration splits</asset>
  </assets>
  <trust_boundaries>
    <boundary>Validated schema-v2 episode to NumPy model-facing arrays</boundary>
    <boundary>Legacy v1 JSONL to explicit compatibility representation</boundary>
    <boundary>Diagnostic actuator data to optional analysis without model-control aliasing</boundary>
  </trust_boundaries>
  <threats>
    <threat severity="HIGH">Padded 8D PWM is returned by `.U`, `action`, `control` or a positional tuple and silently trains a topology-dependent model.</threat>
    <threat severity="HIGH">A v1 adapter renames `action_4d` or infers virtual control/wrench/configuration, creating false v2 evidence.</threat>
    <threat severity="HIGH">Existing v1 dataset/model/MPC defaults are modified to 4D before Phase 8/9 qualification.</threat>
    <threat severity="MEDIUM">Dataset conversion drops episode/configuration/context provenance, enabling row leakage in Phase 8.</threat>
    <threat severity="MEDIUM">Frozen dataclasses expose mutable NumPy buffers whose later mutation changes evidence.</threat>
  </threats>
  <mitigations>
    <mitigation>Use a separately named v2 type with named constructor fields and an invariant `.U is virtual_control_4` of width 4.</mitigation>
    <mitigation>Put PWM/mask/wrench behind `ActuatorDiagnostics`; provide no default `use_pwm_as_u`, `action`, `control` or positional alias.</mitigation>
    <mitigation>Wrap only the existing strict v1 loader; expose source schema, missing fields and false eligibility with no `to_v2` API.</mitigation>
    <mitigation>Retain per-row episode/configuration/context metadata and copy arrays into read-only storage.</mitigation>
    <mitigation>Source/behavior tests lock unchanged v1 dimensions and protected files.</mitigation>
  </mitigations>
  <blocked_on severity="HIGH">Any path that silently returns PWM as v2 U, promotes v1, or changes v1 model/MPC defaults blocks this plan.</blocked_on>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN named DatasetV2 and diagnostics separation</name>
  <files>tests/fixtures/koopman_v2_three_topologies/, tests/test_koopman_dataset_v2.py, koopman/dataset_v2.py</files>
  <read_first>
    <file>koopman/schema_v2.py</file>
    <file>koopman/dataset.py</file>
    <file>tests/test_koopman_dataset.py</file>
    <file>tests/fixtures/koopman_step_small.jsonl</file>
    <file>.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-SPEC.md</file>
  </read_first>
  <action>
RED first: create a deterministic committed local-contract fixture directory containing three independent schema-v2 episode pairs: `base.jsonl` + `base.manifest.json`, `uuv6.jsonl` + `uuv6.manifest.json`, and `uuv4.jsonl` + `uuv4.manifest.json` (at least two contiguous rows per file). One JSONL must contain exactly one configuration/episode, preserving the 07-01 invariant; the three pairs share the same 4D control meaning but use 8/6/4 masks and diagnostic PWM. Generate them through schema builders in a one-time test helper or verify every committed byte/hash; do not hand-label them server evidence. Add tests requiring `KoopmanDatasetV2.X=(n,11)`, `.U=(n,4)`, `R=(n,5)`, `Y=(n,11)`, `U == virtual_control_4`, named per-row configuration/episode/context provenance, and separate `ActuatorDiagnostics` containing PWM `(n,8)`, mask `(n,8)`, wrench `(n,6)`, saturation and energy proxy. Add failures for mixing configurations into one episode, empty/mismatched rows, invalid dimensions/nonfinite data, context/provenance loss, attempt to pass PWM as control, or mutate returned arrays. Confirm RED because the module is absent; commit tests/fixture first.

GREEN: implement `koopman/dataset_v2.py` as explicit v2 types loaded only from a strict schema-v2 episode/manifest pair or validated records. Copy arrays to canonical NumPy dtype, make storage read-only, validate common row count and exact widths, and define `.U` as a property returning only `virtual_control_4`. Keep diagnostics and contexts named; no default flag may select PWM as U and no ambiguous positional tuple/`action`/`control` alias may point at PWM. Preserve episode/configuration/provenance per row so Phase 8 can group without re-reading unvalidated JSON.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_dataset_v2.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>All three independent topology episode/manifest pairs validate separately and load with `.U.shape[1] == 4`; diagnostics PWM/masks remain `(n,8)`.</criterion>
    <criterion>uuv4 padding is zero and mask is retained; no diagnostic field is exposed as the default learned-control array.</criterion>
    <criterion>Configuration, episode id, step index and both context envelopes survive conversion and remain row-addressable.</criterion>
    <criterion>Caller mutation cannot change dataset/diagnostic stored arrays or manifest-derived metadata.</criterion>
    <criterion>Fixture manifests have valid hashes and evidence level `local_contract`, never `server_isaac_smoke`.</criterion>
  </acceptance_criteria>
  <done>A strict immutable v2 dataset provides universal 4D U and separately named actuator diagnostics across 8/6/4 topology fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN explicit non-promoting v1 compatibility</name>
  <files>tests/test_koopman_v1_compatibility.py, koopman/v1_compatibility.py</files>
  <read_first>
    <file>koopman_data.py</file>
    <file>koopman/dataset.py</file>
    <file>koopman/schema_v2.py</file>
    <file>tests/test_koopman_data.py</file>
    <file>tests/fixtures/koopman_step_small.jsonl</file>
  </read_first>
  <action>
RED first: using the canonical v1 fixture, require an explicit `load_v1_compatibility()` entry point to call the existing v1 loader and return a frozen/read-only `V1CompatibilityView` with exact `source_schema="v1"`, `eligible_for_v2_cross_configuration_training=False`, and an explicit unavailable-field set containing virtual control, thruster mask, applied wrench, platform/oracle/estimated context, configuration identity and episode provenance. Test that raw v1 records fail strict v2 validation, the compatibility view cannot be passed to DatasetV2/schema builders, and the public API has no `to_v2`, inferred configuration/control/wrench or promotion method. Add corrupt-v1 tests proving current v1 validation still owns rejection.

GREEN: implement `koopman/v1_compatibility.py` by delegating to `koopman_data.load_koopman_samples`; copy/freeze the resulting view without changing field names or values. Do not infer from `action_4d`/`pwm_8d`, do not import v2 builders to create records, and do not add writer/migration functionality. Any eligibility check must fail closed with a stable explicit reason.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_v1_compatibility.py tests/test_koopman_data.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The canonical v1 fixture is readable only as a source_schema=v1 view with false v2 training eligibility.</criterion>
    <criterion>Raw v1 and compatibility-view objects both fail strict schema-v2/DatasetV2 entry points.</criterion>
    <criterion>The adapter does not expose `to_v2`, guess configuration, rename action to virtual control or reconstruct wrench.</criterion>
    <criterion>Malformed v1 input still fails through the existing v1 loader with no permissive fallback.</criterion>
    <criterion>Existing v1 writer/loader/dataset behavior and fixture bytes remain unchanged.</criterion>
  </acceptance_criteria>
  <done>v1 evidence remains readable through a named read-only compatibility path but cannot be relabelled or promoted to v2.</done>
</task>

<task type="auto">
  <name>Task 3: Lock dual-version isolation and protected regressions</name>
  <files>tests/test_koopman_dataset_v2.py, tests/test_koopman_v1_compatibility.py</files>
  <read_first>
    <file>koopman/dataset_v2.py</file>
    <file>koopman/v1_compatibility.py</file>
    <file>koopman/model.py</file>
    <file>koopman/lifted_edmd.py</file>
    <file>koopman/mpc.py</file>
    <file>koopman/mpc_controller.py</file>
  </read_first>
  <action>
Add paired regression tests proving v1 `KoopmanDataset.U` remains width 8 while v2 `.U` is width 4, and that v1 `KoopmanModel`/lifted EDMD/MPC defaults still derive from `PWM_DIM=8`. Assert Phase 7 adds no import/call from current v1 model/MPC consumers to DatasetV2; Phase 8/9 remain the only migration stages. Run all schema/dataset/compatibility/v1 tests, compileall and protected diff/fixture-hash checks. Confirm local tests do not create canonical server evidence.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_schema_v2.py tests/test_koopman_dataset_v2.py tests/test_koopman_v1_compatibility.py tests/test_koopman_data.py tests/test_koopman_dataset.py tests/test_koopman_model.py tests/test_koopman_mpc.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Paired behavior tests prove v1 U=8 and v2 U=4 without modifying v1 class/model/MPC files.</criterion>
    <criterion>All targeted schema, v2, compatibility and v1 tests pass.</criterion>
    <criterion>Compileall, fixture hash, `git diff --check` and protected-history/model/MPC checks pass.</criterion>
    <criterion>No Phase 8 training or Phase 9 MPC consumption code is introduced.</criterion>
    <criterion>CONT-02, CONT-03 and CONT-05 are covered by executable local tests.</criterion>
  </acceptance_criteria>
  <done>The v1 and v2 data paths coexist with explicit dimensions, semantics and non-promotion boundaries.</done>
</task>

</tasks>

<verification>
1. RED tests initially fail because DatasetV2 and compatibility modules are absent, then minimal GREEN implementations pass.
2. Three topology local fixtures are strict-schema/hash valid and labelled local_contract.
3. v2 `.U=(n,4)` from virtual control; PWM/mask/wrench remain diagnostics only.
4. v1 fixture remains readable but never v2 eligible; raw/view objects fail strict v2 paths.
5. v1 data/dataset/model/MPC regression, compileall, protected diff and whitespace checks pass.
</verification>

<success_criteria>
- Default v2 learned-control meaning cannot drift back to padded PWM.
- Diagnostics and context remain available without polluting `.U`.
- Existing v1 evidence is explicitly readable and explicitly ineligible for v2 cross-configuration claims.
- v1 model/MPC defaults stay frozen until Phase 8/9.
- No HIGH threat remains unresolved.
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-03-SUMMARY.md` with RED/GREEN commits, fixture hashes, U=4/U=8 evidence, adapter non-promotion tests, regression results, deviations and threat dispositions.
</output>
