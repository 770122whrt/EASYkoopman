---
phase: 07-cross-configuration-koopman-data-and-control-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - koopman/schema_v2.py
  - workflows/validate_koopman_v2.py
  - tests/test_koopman_schema_v2.py
autonomous: true
requirements: [CONT-01, CONT-04]
decisions: [D-08, D-09, D-10, D-11, D-12]
user_setup: []
must_haves:
  truths:
    - "D-10: schema v2 has one exact version and rejects missing/extra fields, wrong types/shapes, non-finite values, topology drift and ambiguous provenance"
    - "D-11: one JSONL file represents one contiguous episode whose invariant provenance, step/time and next-state continuity are verified"
    - "D-08/D-09: platform context comes from the canonical configuration contract, while oracle and estimated environment context are structurally and semantically separate"
    - "D-12: every artifact created by these tests and local CLIs remains local_contract and cannot self-assert server origin"
  artifacts:
    - path: koopman/schema_v2.py
      provides: pure-Python transition, episode-manifest, logger/loader and strict validation contract
      contains: "KOOPMAN_TRANSITION_SCHEMA_V2"
    - path: workflows/validate_koopman_v2.py
      provides: deterministic reason-coded validator CLI for transition JSONL plus manifest
      contains: "def main"
    - path: tests/test_koopman_schema_v2.py
      provides: positive exact-eight fixtures and negative mutation/continuity/serialization tests
      contains: "test_exact_schema_v2_transition_is_valid"
  key_links:
    - from: koopman/schema_v2.py
      to: easyuuv_nc/embodiments.py
      via: lazy topology validation using qualification_record instead of a copied configuration table
      pattern: "qualification_record"
    - from: workflows/validate_koopman_v2.py
      to: koopman/schema_v2.py
      via: strict loader plus transition/episode/manifest validators
      pattern: "validate_koopman"
---

<objective>
用严格 TDD 建立无 Isaac/Torch 依赖的 schema v2 核心：精确字段、context/provenance、单条 transition、连续 episode、JSONL logger/loader、manifest/hash 与验证 CLI。

Purpose: 先固定 Phase 7 唯一数据事实，使后续 Bridge、DatasetV2 和服务器 runner 都只能产出同一种可失败、可审计记录。
Output: `koopman/schema_v2.py`、validator CLI 和完整 mutation test matrix。
</objective>

<execution_context>
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-SPEC.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-CONTEXT.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-RESEARCH.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-PATTERNS.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@koopman_data.py
@workflows/easyuuv_v2_qualification_artifact.py
@workflows/validate_easyuuv_v2_qualification.py
@workflows/merge_easyuuv_v2_qualification.py
@easyuuv_nc/embodiments.py
@tests/test_koopman_data.py
@tests/test_easyuuv_v2_qualification.py
</context>

<threat_model>
  <assets>
    <asset>Schema v2 field/control meaning and exact version identity</asset>
    <asset>Episode JSONL, manifest, SHA-256 and evidence-level provenance</asset>
    <asset>Canonical Phase 6 topology truth and frozen v1 data contract</asset>
  </assets>
  <trust_boundaries>
    <boundary>Untrusted JSON/JSONL bytes to validated in-memory transition objects</boundary>
    <boundary>Per-row validated data to episode-level canonical artifact promotion</boundary>
    <boundary>Configuration strings/context values to canonical catalog comparison</boundary>
  </trust_boundaries>
  <threats>
    <threat severity="HIGH">A v1, partial or hand-written record is accepted as schema v2 by permissive missing/extra/version handling.</threat>
    <threat severity="HIGH">A local fixture self-labels as server evidence and is later consumed as runtime proof.</threat>
    <threat severity="HIGH">Episode rows are individually valid but duplicated, reordered, discontinuous or mixed across configurations.</threat>
    <threat severity="MEDIUM">NaN/Infinity, duplicate JSON keys, bool-as-int or oversized input bypasses ordinary Python/JSON checks.</threat>
    <threat severity="MEDIUM">A failed finalize overwrites the last valid manifest or exposes a partial file as canonical.</threat>
  </threats>
  <mitigations>
    <mitigation>Exact key-set and exact schema-identifier equality at every object layer; reject unknown versions and semantic aliases.</mitigation>
    <mitigation>Bound input bytes/line count, reject duplicate keys and JSON constants, exclude bool from integer validation and require all numeric values finite.</mitigation>
    <mitigation>Separate transition, episode and manifest validators with invariant, step/time, next-state and hash checks.</mitigation>
    <mitigation>Local builders can emit only local_contract; server provenance is validated later together with external sidecars/logs.</mitigation>
    <mitigation>Write `.part` first; validate complete episode, fsync and atomically replace only after manifest/hash success.</mitigation>
  </mitigations>
  <blocked_on severity="HIGH">Any unresolved HIGH threat or a validator mutation that passes unexpectedly blocks completion of this plan.</blocked_on>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN the exact transition and context contract</name>
  <files>tests/test_koopman_schema_v2.py, koopman/schema_v2.py</files>
  <read_first>
    <file>.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-SPEC.md</file>
    <file>.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-CONTEXT.md</file>
    <file>koopman_data.py</file>
    <file>workflows/easyuuv_v2_qualification_artifact.py</file>
    <file>easyuuv_nc/embodiments.py</file>
  </read_first>
  <action>
RED first: create a canonical transition fixture factory and failing tests for the exact 13 top-level fields locked in `07-RESEARCH.md`, exact identifier `easyuuv-koopman-transition-v2`, fixed vector widths 11/5/4/4/8/8/6/11, finite/range/type checks, non-bool integer fields, exact nested key sets, 40-hex source commit and valid local evidence. Add parameterized single-mutation failures for missing/extra key, wrong/ambiguous version, NaN/Inf, wrong shape, out-of-range action/PWM, non-binary mask, nonzero padding, unknown configuration, catalog mask/count/rank conflict, `uuv4*` nonzero virtual yaw, wrench/context/provenance errors, oracle labelled as estimate and available estimated context without deployable method/source signals. Run the file and record that it fails because `koopman.schema_v2` does not yet exist; commit the RED test separately.

GREEN: create pure-stdlib `koopman/schema_v2.py`. Define exact immutable constants and builders that return fresh dictionaries; validators must not mutate caller objects. Use lazy `easyuuv_nc.embodiments.qualification_record()` only for topology identity—do not copy the eight-config table and do not import Gym/Torch/Isaac at module import. Emit stable `ValueError("reason:detail")` families documented in research. Keep estimated context `available=false` legal with empty values/provenance; when available, require a non-oracle source kind, method/version and source signals. Enforce `motor_pwm_padded_8[i] == 0` wherever mask is zero and `virtual_control_4[2] == 0` for `uuv4*`.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_schema_v2.py -k "transition or context or topology"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`koopman/schema_v2.py` imports in a subprocess without adding `torch`, `gymnasium`, `omni` or `easyuuv_nc.env` to `sys.modules`.</criterion>
    <criterion>The canonical fixture passes for each of the exact eight public configurations; `heavy_duty` and every single-field mutation fail with a stable reason prefix.</criterion>
    <criterion>The implementation contains one exact transition version constant and no permissive numeric/prefix version branch.</criterion>
    <criterion>Oracle and estimated objects cannot alias/copy provenance, and estimated unavailable remains a valid explicit state.</criterion>
    <criterion>D-08, D-09 and D-10 are enforced by behavior tests, not only source-text assertions.</criterion>
  </acceptance_criteria>
  <done>Strict schema v2 transition/context validation is green across exact-eight positives and the full negative mutation matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN contiguous episode logging, manifest and validator CLI</name>
  <files>tests/test_koopman_schema_v2.py, koopman/schema_v2.py, workflows/validate_koopman_v2.py</files>
  <read_first>
    <file>koopman/schema_v2.py</file>
    <file>koopman_data.py</file>
    <file>workflows/merge_easyuuv_v2_qualification.py</file>
    <file>workflows/validate_easyuuv_v2_qualification.py</file>
    <file>tests/test_easyuuv_v2_qualification_runner.py</file>
  </read_first>
  <action>
RED first: add failing tests for one-episode-only JSONL, exact `step_index + 1`, `simulation_time_s + control_dt_s`, next-state→next-row-state continuity, invariant configuration/scenario/episode/seed/dt/task/controller/source/evidence, duplicate/missing/out-of-order rows, platform digest drift and configurable numeric tolerance that cannot hide a missing step. Add bounded loader tests for oversize bytes/line, duplicate JSON keys, `NaN/Infinity`, invalid UTF-8 and `.part` refusal. Add logger tests proving row validation before write, failed finalize leaves `.part`, valid finalize produces deterministic JSONL plus an atomically written manifest with record count, first/last step/time, episode invariants, SHA-256 and `local_contract`.

GREEN: implement episode validator, `KoopmanEpisodeLoggerV2`, bounded JSONL loader and exact manifest version in `schema_v2.py`. Do not support append/reuse across episodes. Use same-directory temporary files, `flush`, `os.fsync` and `os.replace`; compute SHA-256 from finalized transition bytes. Create `workflows/validate_koopman_v2.py` with `--jsonl`, `--manifest`, optional `--json`, deterministic stdout and nonzero exit for any strict failure. The CLI must not offer a flag that upgrades local evidence to server evidence.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_schema_v2.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Valid two-or-more-row episodes round-trip deterministically and their manifest hash matches the exact JSONL bytes.</criterion>
    <criterion>Duplicate/reordered/missing steps, time drift, invariant drift, state discontinuity and hash mismatch each fail with distinct reason codes.</criterion>
    <criterion>Duplicate JSON keys, JSON constants, invalid UTF-8, oversize inputs and `.part` files are rejected before returning records.</criterion>
    <criterion>A failed write/finalize never replaces an existing canonical JSONL/manifest pair.</criterion>
    <criterion>Validator CLI returns 0 only for the exact valid pair and prints machine-readable local evidence without a server-pass claim.</criterion>
  </acceptance_criteria>
  <done>One immutable contiguous episode can be logged, hashed, atomically finalized, loaded and strictly validated without Isaac.</done>
</task>

<task type="auto">
  <name>Task 3: Lock Isaac-free import, v1 isolation and plan-level regression</name>
  <files>tests/test_koopman_schema_v2.py</files>
  <read_first>
    <file>koopman/schema_v2.py</file>
    <file>workflows/validate_koopman_v2.py</file>
    <file>koopman_data.py</file>
    <file>koopman/dataset.py</file>
    <file>koopman/model.py</file>
    <file>koopman/mpc.py</file>
  </read_first>
  <action>
Add final source/behavior contracts that cold-import schema and CLI in a subprocess and assert no Isaac/Torch/Gym task modules load. Assert v1 required fields, `PWM_DIM=8`, `KoopmanDataset.U` shape contract and model/MPC defaults remain untouched. Run targeted Phase 6 schema/catalog tests alongside the new file to prove the new exact-version/topology validation did not fork catalog truth. Run compileall and protected-history diff checks; do not write any file under `source/results/koopman_phase7`.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_schema_v2.py tests/test_koopman_data.py tests/test_koopman_dataset.py tests/test_easyuuv_v2_catalog.py tests/test_easyuuv_v2_qualification.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>All targeted new/v1/Phase 6 tests pass and cold imports remain Isaac/Torch/Gym free.</criterion>
    <criterion>`python -m compileall koopman/schema_v2.py workflows/validate_koopman_v2.py tests/test_koopman_schema_v2.py` exits 0.</criterion>
    <criterion>`git diff v1.0 -- .planning/milestones .planning/reports koopman/model.py koopman/mpc.py` shows no Phase 7 change to protected archives or v1 model/MPC files.</criterion>
    <criterion>No canonical Phase 7 server result directory or success artifact is created by local tests.</criterion>
    <criterion>CONT-01 and the schema portion of CONT-04 have executable local evidence.</criterion>
  </acceptance_criteria>
  <done>The schema core is locally qualified, does not load Isaac, and leaves v1/Phase 6 contracts intact.</done>
</task>

</tasks>

<verification>
1. RED commits demonstrate each test group initially fails for the intended missing semantic, followed by minimal GREEN commits.
2. `.venv\Scripts\python.exe -m pytest -q tests/test_koopman_schema_v2.py` exits 0 with the full mutation matrix.
3. Cold import proves schema/CLI are pure Python and do not import Torch/Gym/Omni/environment modules.
4. Targeted v1 and Phase 6 regressions pass; protected archive/model/MPC diff is empty.
5. `git diff --check` and compileall exit 0; no `source/results/koopman_phase7` success artifact exists.
</verification>

<success_criteria>
- An exact, versioned and immutable schema v2 contract covers all CONT-01 fields.
- Context provenance and oracle/estimated separation satisfy the structural portion of CONT-04.
- Per-row validity cannot hide episode discontinuity, invariant drift or hash mismatch.
- Local evidence cannot self-promote to server evidence.
- No HIGH threat remains unresolved and v1 defaults remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-01-SUMMARY.md` with RED/GREEN commits, exact schema identifiers, reason-code coverage, test counts, v1/protected-path checks, deviations and threat dispositions.
</output>
