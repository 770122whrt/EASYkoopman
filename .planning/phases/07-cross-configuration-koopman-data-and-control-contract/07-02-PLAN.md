---
phase: 07-cross-configuration-koopman-data-and-control-contract
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - easyuuv_nc/env/easyuuv_env.py
  - workflows/koopman_bridge_v2.py
  - tests/test_koopman_bridge_v2.py
autonomous: true
requirements: [CONT-01, CONT-02, CONT-04]
decisions: [D-04, D-05, D-06, D-07, D-08, D-09, D-11, D-12]
user_setup: []
must_haves:
  truths:
    - "D-04/D-05: runtime exposes a distinct post-mask/pre-TAM virtual_control_4; uuv4* nonzero raw yaw becomes exact zero virtual yaw before allocation"
    - "D-07: applied_wrench_6 is measured after actuator dynamics/efficiency and before hydrodynamic, buoyancy, boundary or pulse forces are mixed in"
    - "D-08/D-09: Bridge records live per-episode platform values and same-step oracle context, while estimated context remains explicitly unavailable"
    - "D-11: one Bridge call binds pre-state/reference/action, one env.step, same-step telemetry and post-state with a freshness token"
  artifacts:
    - path: easyuuv_nc/env/easyuuv_env.py
      provides: additive read-only raw/virtual/PWM/wrench/fluid/efficiency telemetry and reset/freshness semantics
      contains: "_last_virtual_control_4"
    - path: workflows/koopman_bridge_v2.py
      provides: atomic EasyUUV runtime-to-schema-v2 transition Bridge
      contains: "class KoopmanBridgeV2"
    - path: tests/test_koopman_bridge_v2.py
      provides: exact-eight mask/TAM, actual-wrench capture and atomic-step tests without Isaac
      contains: "test_uuv4_nonzero_raw_yaw_is_masked_before_tam"
  key_links:
    - from: easyuuv_nc/env/easyuuv_env.py
      to: easyuuv_nc/embodiments.py
      via: active configuration qualification_record-derived control mask
      pattern: "control_mask"
    - from: workflows/koopman_bridge_v2.py
      to: koopman/schema_v2.py
      via: build and strict-validate one transition only after fresh telemetry is complete
      pattern: "build_koopman_transition_v2"
---

<objective>
在不改变既有 v1/Phase 6 API 的前提下，为 EasyUUV 2.0 增加真实、同一步、只读的控制/执行器/context 遥测，并用原子 Koopman Bridge 组装 schema v2 transition。

Purpose: 把 Phase 7 的字段语义接到真实控制和物理调用链，而不是从 action、TAM 或 PWM 事后猜测。
Output: 最小环境遥测增量、`KoopmanBridgeV2` 和 exact-eight Isaac-free 行为合同。
</objective>

<execution_context>
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-SPEC.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-CONTEXT.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-RESEARCH.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-PATTERNS.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-01-SUMMARY.md
</execution_context>

<context>
@koopman/schema_v2.py
@easyuuv_nc/env/easyuuv_env.py
@easyuuv_nc/embodiments.py
@easyuuv_nc/thrust_allocation.py
@workflows/koopman_logging.py
@workflows/qualify_easyuuv_v2.py
@tests/test_easyuuv_v2_catalog.py
@tests/test_easyuuv_v2_qualification_runner.py
</context>

<threat_model>
  <assets>
    <asset>EasyUUV control and dynamics behavior already qualified by Phase 6</asset>
    <asset>Post-mask/pre-TAM virtual-control semantic and actual thruster-only wrench</asset>
    <asset>Atomic association between transition fields and a single physics step</asset>
  </assets>
  <trust_boundaries>
    <boundary>Caller action to EasyUUV pre-physics/PID/TAM pipeline</boundary>
    <boundary>Mutable runtime `_last_*` buffers to immutable Bridge transition</boundary>
    <boundary>Static catalog baseline to live per-environment randomized platform/context values</boundary>
  </trust_boundaries>
  <threats>
    <threat severity="HIGH">`_last_pid_value` is relabelled as virtual control even though uuv4 yaw is only implicitly ignored inside WLS.</threat>
    <threat severity="HIGH">Applied wrench is reconstructed from desired wrench/PWM or read after environmental forces are mixed, changing its physical meaning.</threat>
    <threat severity="HIGH">Stale or off-by-one telemetry pairs state/action from one step with motor/wrench/context from another.</threat>
    <threat severity="MEDIUM">A telemetry change mutates Phase 6 behavior, allocation output or reset semantics.</threat>
    <threat severity="MEDIUM">Bridge silently reads env 0 from a batched environment or substitutes missing values with zeros/catalog defaults.</threat>
  </threats>
  <mitigations>
    <mitigation>Apply a catalog-derived 4D mask once before TAM; cache and allocate from the same masked tensor while preserving legacy `_last_pid_value`.</mitigation>
    <mitigation>Cache force/torque immediately after per-thruster sum and before hydrodynamics; cache fluid velocity/efficiency used by that same dynamics call.</mitigation>
    <mitigation>Reset telemetry validity and increment a runtime step token only when every required post-step cache has been produced.</mitigation>
    <mitigation>Require explicit env index and exact batch/shape match; absence/staleness is a hard reason-coded failure.</mitigation>
    <mitigation>Cross-check allocation output against the pre-change WLS result for uuv4 and run Phase 6 catalog/runner regressions.</mitigation>
  </mitigations>
  <blocked_on severity="HIGH">If source audit cannot identify an unambiguous capture point or any control/allocation regression remains, stop rather than weakening field semantics.</blocked_on>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN additive runtime truth telemetry</name>
  <files>tests/test_koopman_bridge_v2.py, easyuuv_nc/env/easyuuv_env.py</files>
  <read_first>
    <file>.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-RESEARCH.md</file>
    <file>easyuuv_nc/env/easyuuv_env.py</file>
    <file>easyuuv_nc/embodiments.py</file>
    <file>easyuuv_nc/thrust_allocation.py</file>
    <file>tests/test_easyuuv_v2_catalog.py</file>
  </read_first>
  <action>
RED first: create pure/fake-runtime tests that lock the exact source semantics before implementation. Test all eight catalog configurations for `[roll,pitch,yaw,depth]`, mask shape/rank and N=8/6/4 allocation. For `uuv4` and `uuv4_angled`, supply nonzero raw/PID yaw and require exact zero virtual yaw before TAM while the resulting WLS motor vector stays equivalent to the prior zero-weight-yaw allocation. Add source/isolated tests requiring raw action capture before `_actions` overwrite/clip, thruster-only wrench cache after conversion/lag/efficiency/ventilation and force/torque sum but before fluid/buoyancy/boundary/pulse addition, same-call fluid/efficiency cache, reset invalidation and monotonically increasing step token. Confirm RED fails because new telemetry is absent; commit tests separately.

GREEN: minimally modify `easyuuv_nc/env/easyuuv_env.py` without splitting `EasyUUVEnvCfg` or changing public task registration. Preserve `_last_pid_value`; add separate `_last_raw_action_4`, `_last_virtual_control_4`, `_last_applied_wrench_6`, `_last_fluid_velocity_w`, actual N-channel efficiency cache and a freshness/step token. Derive `_control_mask_4` from the active catalog/configuration application, multiply after PID priority/deadband processing and before sign/TAM, and make the same masked tensor feed TAM. Cache raw input before clip/overwrite. Cache actual force/torque after PWM mapping, first-order dynamics, conversion and runtime efficiency, immediately after per-thruster sum; do not include hydrodynamic/boundary forces. Reset buffers to unavailable/zero plus invalid token. Expose clone-returning read-only getters or one telemetry snapshot method so Bridge need not depend on writable tensor references.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_bridge_v2.py -k "telemetry or mask or wrench or token"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>All exact-eight configurations share a 4D virtual-control channel order; motor allocation lengths remain 8/6/4 and rank/masks match Phase 6.</criterion>
    <criterion>Both uuv4 variants accept a nonzero raw yaw test input, expose exact zero virtual yaw before TAM and preserve equivalent motor allocation.</criterion>
    <criterion>Actual wrench telemetry is populated only after actuator dynamics/efficiency and excludes fluid/buoyancy/boundary/pulse forces.</criterion>
    <criterion>Fluid velocity and efficiency are the values used by the same dynamics call, not recomputed after step.</criterion>
    <criterion>Reset invalidates telemetry and no getter returns a writable alias to internal tensors.</criterion>
  </acceptance_criteria>
  <done>The environment exposes fresh, physically correct, additive telemetry without changing Phase 6 allocation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN one-step atomic Koopman Bridge</name>
  <files>tests/test_koopman_bridge_v2.py, workflows/koopman_bridge_v2.py</files>
  <read_first>
    <file>koopman/schema_v2.py</file>
    <file>workflows/koopman_logging.py</file>
    <file>workflows/qualify_easyuuv_v2.py</file>
    <file>easyuuv_nc/env/easyuuv_env.py</file>
    <file>tests/test_easyuuv_v2_qualification_runner.py</file>
  </read_first>
  <action>
RED first: build a fake batched env whose state, reference and telemetry carry distinct step tokens. Require `KoopmanBridgeV2.step_and_record(raw_action_4, ...)` to capture pre-step state/reference/provenance, call `env.step` exactly once, require a strictly newer complete runtime token, extract the requested env index, build live platform context and same-step oracle context, set estimated context explicitly unavailable, capture post-state, and return a strict schema-v2 transition. Add failures for missing/stale token, absent telemetry, wrong batch/shape, non-finite value, env-index escape, state/reference dimension drift, configuration mismatch and semantic swaps. Verify consecutive calls use the prior `next_state_11` as the next pre-state and do not resample inconsistently.

GREEN: implement `workflows/koopman_bridge_v2.py` with explicit constructor inputs for env, env index, scenario/episode/seed/task/controller/source/evidence and no hidden server claim. Reuse or parity-test the current 11D state/5D reference extraction from `workflows/koopman_logging.py`; do not modify it. Topology identity comes from `qualification_record`, while mass/inertia/COM/COB/volume/drag/tau come from active live buffers for the selected env. Build oracle context from cached fluid velocity, water density/viscosity, drag and actual efficiency with units/frames. Never substitute missing runtime facts with catalog defaults or desired wrench. Call the schema builder/validator before returning or logging.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_bridge_v2.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>A fake step produces exactly one valid transition with pre-state, raw action, post-mask virtual control, clipped padded PWM/mask, actual wrench and post-state from one token.</criterion>
    <criterion>Consecutive transitions satisfy state/step/time continuity without a tolerance workaround or duplicate sampling.</criterion>
    <criterion>Missing/stale/wrong-batch telemetry is a hard failure; Bridge does not fill zeros, use raw action as control or reconstruct wrench.</criterion>
    <criterion>Platform continuous values come from the active env index; topology comes from the canonical catalog and agrees with it.</criterion>
    <criterion>Oracle context has explicit units/frames; estimated context is separately present as unavailable and contains no oracle values.</criterion>
  </acceptance_criteria>
  <done>The Bridge atomically converts one EasyUUV step into one strict v2 transition and rejects stale or fabricated telemetry.</done>
</task>

<task type="auto">
  <name>Task 3: Prove control equivalence and run Phase 6/v1 regressions</name>
  <files>tests/test_koopman_bridge_v2.py</files>
  <read_first>
    <file>tests/test_koopman_bridge_v2.py</file>
    <file>tests/test_easyuuv_v2_catalog.py</file>
    <file>tests/test_easyuuv_v2_qualification_runner.py</file>
    <file>tests/test_phase1_source_contract.py</file>
    <file>easyuuv_nc/env/easyuuv_env.py</file>
  </read_first>
  <action>
Add regression assertions that all six fully controllable configurations retain their prior allocation output, both 4-thruster configurations differ only by explicit yaw masking that WLS already made physically ineffective, and default/off context settings preserve current dynamics. Run the Bridge suite together with Phase 6 package/catalog/qualification/runner tests and relevant v1 source contracts. Compile modified/new modules; run protected-path and whitespace checks. Do not start Isaac and label all results local_contract only.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_bridge_v2.py tests/test_easyuuv_v2_package.py tests/test_easyuuv_v2_catalog.py tests/test_easyuuv_v2_qualification.py tests/test_easyuuv_v2_qualification_runner.py tests/test_phase1_source_contract.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Targeted Bridge and Phase 6/v1 regressions all pass without importing/starting Isaac.</criterion>
    <criterion>Existing `_last_pid_value` and Phase 6 `_last_motor_values_clipped` consumers remain valid.</criterion>
    <criterion>Compileall and `git diff --check` exit 0; protected v1 archive/model/MPC paths remain unchanged.</criterion>
    <criterion>Tests cover every public configuration locally but do not claim an actual Isaac transition.</criterion>
    <criterion>CONT-02 and the runtime portions of CONT-01/CONT-04 have executable local evidence.</criterion>
  </acceptance_criteria>
  <done>The new telemetry/Bridge semantics are locally proven and upstream behavior remains regression-safe.</done>
</task>

</tasks>

<verification>
1. RED tests fail for missing explicit mask, actual-wrench/context cache and freshness token before production changes.
2. Exact-eight pure tests prove channel order, topology, mask/rank and allocation equivalence; uuv4 raw yaw→virtual zero is explicit.
3. Fake env tests prove one-call/one-step/one-token atomic alignment and strict schema validation.
4. Phase 6 package/catalog/qualification/runner and v1 source-contract tests pass.
5. Compileall, protected-path diff and `git diff --check` pass; no server evidence is claimed.
</verification>

<success_criteria>
- Runtime truth is measured at the correct control/dynamics points, not reconstructed.
- Virtual control is uniformly 4D and topology mask is applied before TAM.
- Applied wrench excludes environmental forces but includes actual actuator effects.
- Bridge transitions are atomic, fresh, batch-safe and strict-schema valid.
- No HIGH threat or Phase 6/v1 regression remains.
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-02-SUMMARY.md` with RED/GREEN commits, capture-point evidence, exact-eight/uuv4 results, test counts, regression outputs, deviations and threat dispositions.
</output>
