---
phase: 07-cross-configuration-koopman-data-and-control-contract
plan: "04"
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - workflows/collect_koopman_v2_smoke.py
  - workflows/merge_koopman_v2_evidence.py
  - workflows/validate_koopman_v2.py
  - scripts/phase7_local_preflight.ps1
  - scripts/phase7_prepare_bundle.ps1
  - scripts/phase7_server_bootstrap.sh
  - scripts/phase7_server_smoke.sh
  - scripts/phase7_pullback.ps1
  - tests/test_phase7_server_evidence_contract.py
  - docs/phase7_koopman_v2_runbook.md
  - source/results/koopman_phase7/
  - .planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-SERVER-EVIDENCE.md
autonomous: false
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05]
decisions: [D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16]
user_setup:
  - "When requested after all local gates pass, ensure the unchanged Isaac server is powered on and SSH-reachable; the agent performs transfer and server commands directly."
  - "Keep the existing Isaac Sim 5.0 + Isaac Lab 2.2.1 installation and `/root/IsaacLab`; Phase 7 uses a new isolated `/root/EASYkoopman-phase7-v2` checkout."
must_haves:
  truths:
    - "D-12/D-13: local fixtures prove contracts only; actual server processes produce at least eight contiguous v2 transitions for exact representatives base, uuv6 and uuv4"
    - "D-05: the uuv4 server episode includes a nonzero raw-yaw request and records zero post-mask virtual yaw before TAM"
    - "D-07/D-09: server rows contain actual post-actuator thruster-only wrench and same-step oracle context, never desired/reconstructed values"
    - "D-14: tested source commit, runtime versions, native/tee/semantic status, logs, file hashes and pullback validator all agree before evidence promotion"
    - "D-15/D-16: implementation, pulled server results and planning closure are auditable boundaries; absent/partial/stale evidence keeps Phase 7 open"
  artifacts:
    - path: workflows/collect_koopman_v2_smoke.py
      provides: one-configuration real Isaac Bridge episode collector with deterministic semantic probes
      contains: "--configuration"
    - path: scripts/phase7_server_smoke.sh
      provides: fail-closed three-process base/uuv6/uuv4 server orchestration, validation, logs and hashes
      contains: "uuv4"
    - path: scripts/phase7_pullback.ps1
      provides: staged SCP/hash/source/runtime/validator checks before atomic local evidence promotion
      contains: "sha256_mismatch"
    - path: docs/phase7_koopman_v2_runbook.md
      provides: exact local, offline transfer, server, failure and pullback procedure
      contains: "## Server Three-Topology Smoke"
    - path: .planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-SERVER-EVIDENCE.md
      provides: actual post-checkpoint evidence index and CONT-01..05 mapping
      contains: "phase7_gate: pass"
  key_links:
    - from: workflows/collect_koopman_v2_smoke.py
      to: workflows/koopman_bridge_v2.py
      via: one Bridge step per deterministic action and strict episode logger
      pattern: "KoopmanBridgeV2"
    - from: scripts/phase7_pullback.ps1
      to: workflows/validate_koopman_v2.py
      via: validate staged exact commit/hash artifacts before canonical promotion
      pattern: "validate_koopman_v2.py"
---

<objective>
在本地全量门全部通过后，把 schema/Bridge 接到真实 Isaac 服务器，对 8/6/4 三类拓扑分别生成连续 v2 episode，并通过离线 bundle、隔离 checkout、进程/日志/语义门、hash pullback 与独立 validator 形成 Phase 7 完成证据。

Purpose: 证明 Phase 7 的数据与控制契约在真实 simulator 调用链中成立；不重复 Phase 6 八构型资格测试，也不宣称 Koopman 模型预测/控制效果。
Output: runner/merger、fail-closed scripts、runbook、三拓扑 server artifact、`07-SERVER-EVIDENCE.md` 及最终 phase verification 输入。
</objective>

<execution_context>
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-SPEC.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-CONTEXT.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-RESEARCH.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-PATTERNS.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-01-SUMMARY.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-02-SUMMARY.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-03-SUMMARY.md
</execution_context>

<context>
@koopman/schema_v2.py
@workflows/koopman_bridge_v2.py
@workflows/validate_koopman_v2.py
@workflows/qualify_easyuuv_v2.py
@scripts/phase6_prepare_bundle.ps1
@scripts/phase6_server_bootstrap.sh
@scripts/phase6_server_preflight.sh
@scripts/phase6_offline_install.sh
@scripts/phase6_server_qualification.sh
@scripts/phase6_pullback.ps1
@docs/phase6_easyuuv_v2_qualification_runbook.md
@tests/test_easyuuv_v2_qualification_runner.py
</context>

<threat_model>
  <assets>
    <asset>Tested Phase 7 source commit and unchanged Isaac server installation</asset>
    <asset>Three topology JSONL/manifest/log artifacts and aggregate hashes</asset>
    <asset>Canonical local `source/results/koopman_phase7` evidence and frozen v1/Phase 6 evidence</asset>
  </assets>
  <trust_boundaries>
    <boundary>Clean local tested checkout to offline Git bundle and commit sidecar</boundary>
    <boundary>Server shell/process/log outputs to per-configuration semantic evidence</boundary>
    <boundary>Remote evidence archive/hash to staged local validation and canonical promotion</boundary>
  </trust_boundaries>
  <threats>
    <threat severity="HIGH">A local fixture, stale Phase 6 file or hand-edited JSON is relabelled as Phase 7 server evidence.</threat>
    <threat severity="HIGH">One topology/process/log fails but merge omits it or an old output survives and is validated instead.</threat>
    <threat severity="HIGH">Executed server checkout/runtime differs from the locally tested commit or expected Isaac 5.0/Lab 2.2.1.</threat>
    <threat severity="HIGH">SCP/hash/validator failure is ignored and an incomplete staging directory is promoted over canonical evidence.</threat>
    <threat severity="MEDIUM">User-controlled configuration/path reaches a shell or escapes the isolated project/result root.</threat>
    <threat severity="MEDIUM">Python succeeds while `tee`/log capture fails, hiding mandatory provenance.</threat>
    <threat severity="MEDIUM">Tracked/untracked Python or bytecode drift changes tested/executed validation code.</threat>
  </threats>
  <mitigations>
    <mitigation>Exact configuration choices, confined resolved paths, fresh non-existing target dirs and no shell construction from Python arguments.</mitigation>
    <mitigation>Clean worktree including unignored untracked files; Git bundle ref/sidecar/local HEAD/server HEAD/source_commit must be the same 40-hex commit.</mitigation>
    <mitigation>Three isolated runner processes, exact-set merge and native plus `PIPESTATUS` tee plus semantic/log gates; any nonzero blocks merge.</mitigation>
    <mitigation>Server preflight records expected/actual Sim/Lab versions and offline editable install with `--no-deps --no-build-isolation --no-index`.</mitigation>
    <mitigation>Pull into a fresh staging dir, compare every sidecar/hash, run the tested strict validator, then atomically promote only when canonical evidence does not exist.</mitigation>
    <mitigation>Do not track Python bytecode; export `PYTHONDONTWRITEBYTECODE=1`; preserve failed/partial rows and logs separately.</mitigation>
  </mitigations>
  <blocked_on severity="HIGH">Any unresolved HIGH threat, unavailable server, failed topology, hash/version/source drift or missing log keeps Phase 7 incomplete; never synthesize a replacement.</blocked_on>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN real-runner and exact-three evidence semantics</name>
  <files>tests/test_phase7_server_evidence_contract.py, workflows/collect_koopman_v2_smoke.py, workflows/merge_koopman_v2_evidence.py, workflows/validate_koopman_v2.py</files>
  <read_first>
    <file>workflows/qualify_easyuuv_v2.py</file>
    <file>workflows/merge_easyuuv_v2_qualification.py</file>
    <file>workflows/koopman_bridge_v2.py</file>
    <file>koopman/schema_v2.py</file>
    <file>tests/test_easyuuv_v2_qualification_runner.py</file>
  </read_first>
  <action>
RED first: add Isaac-free parser/action/merge tests. Require CLI choices exact `base,uuv6,uuv4`, positive `--steps` default/minimum 8, seed/task/scenario/episode/result-root/output and headless arguments, with AppLauncher-before-Gym/Torch/environment import. Deterministic actions must exercise every controllable channel and include at least one nonzero raw yaw for `uuv4`; tests require the resulting Bridge record to have zero virtual yaw. Mock the Bridge/env boundary only for local tests and label all outputs local_contract. Add failures for missing/stale actual telemetry, noncontiguous rows, wrong config/topology/context, desired-vs-actual semantic marker, duplicate/missing/extra input, version/source/log disagreement, path escape/symlink, overwrite and atomic-merge failure. Add CLI RED tests requiring `workflows/validate_koopman_v2.py --aggregate <evidence.json> --json` to validate the exact-three aggregate, every referenced episode/manifest/log hash, common source/runtime provenance and server evidence level; missing/extra references or a local-only aggregate must return nonzero. Commit RED before runner/aggregate support exists.

GREEN: implement one-configuration `workflows/collect_koopman_v2_smoke.py` following the proven AppLauncher order. After create/config application/reset, build a fresh episode id and call `KoopmanBridgeV2` for exactly N steps; finalize JSONL+manifest before closing SimulationApp, or write a distinct strict failure record/log and return nonzero. Never fall back to requested action/desired wrench. Constrain writes under an explicit result root and recheck at write time. Implement `workflows/merge_koopman_v2_evidence.py` to require exact set `{base,uuv6,uuv4}`, individually validate each episode/manifest, require identical source/runtime/evidence metadata and atomically write an aggregate manifest containing file/log hashes. Extend the Plan 07-01 validator CLI with the tested `--aggregate` mode; it must revalidate every referenced byte/hash and exact-three/common-provenance rule rather than trusting merger output. A local mocked merge remains local_contract and cannot pass server final mode without external runtime provenance fields.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_phase7_server_evidence_contract.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Runner help/import works locally without Isaac and lists only base/uuv6/uuv4 plus all required provenance/output arguments.</criterion>
    <criterion>uuv4 deterministic input includes nonzero raw yaw and the mocked real Bridge semantics require zero virtual yaw.</criterion>
    <criterion>Missing/stale telemetry or invalid episode writes failure evidence and native nonzero status; it never creates an eligible success manifest.</criterion>
    <criterion>Merger rejects missing/duplicate/extra topology, mismatched commit/runtime/log/hash and validates all three before atomic replacement.</criterion>
    <criterion>`validate_koopman_v2.py --aggregate ... --json` is implemented and rejects local-only, partial, stale or hash-mismatched aggregate evidence.</criterion>
    <criterion>Output escape, symlink component, existing canonical target and overwrite attempts fail closed.</criterion>
  </acceptance_criteria>
  <done>The real runner and exact-three merger have strong local behavior contracts without claiming Isaac execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN offline server chain, pullback and local completion gate</name>
  <files>tests/test_phase7_server_evidence_contract.py, scripts/phase7_local_preflight.ps1, scripts/phase7_prepare_bundle.ps1, scripts/phase7_server_bootstrap.sh, scripts/phase7_server_smoke.sh, scripts/phase7_pullback.ps1, docs/phase7_koopman_v2_runbook.md</files>
  <read_first>
    <file>scripts/phase6_prepare_bundle.ps1</file>
    <file>scripts/phase6_server_bootstrap.sh</file>
    <file>scripts/phase6_server_preflight.sh</file>
    <file>scripts/phase6_offline_install.sh</file>
    <file>scripts/phase6_server_qualification.sh</file>
    <file>scripts/phase6_pullback.ps1</file>
    <file>docs/phase6_easyuuv_v2_qualification_runbook.md</file>
    <file>.planning/phases/06-easyuuv-2-0-intake-and-multi-configuration-qualification/06-REVIEW-POSTFIX.md</file>
  </read_first>
  <action>
RED first: add dynamic temp-Git-repo/PowerShell/Bash contract tests for all Phase 6 postfix failure classes: tracked or unignored untracked dirty source, local HEAD/sidecar drift, incomplete bundle, existing server target, server checkout not clean, Sim/Lab version drift with persisted expected/actual failure, online PEP517 attempt, missing setuptools, Python/tee/semantic/log failure, exact-three omission, bytecode pollution, stale result reuse, SCP failure, source/Lab tag/commit/hash mismatch, local validator drift and promotion over existing canonical evidence. Add a dynamic local-preflight test that stubs each required command in turn and proves any nonzero targeted/full pytest, collect-only threshold, compileall, pip check, Bash/PowerShell parse, protected diff, dirty worktree or canonical-evidence-presence result stops before the bundle command is invoked. Ensure tests fail because Phase 7 scripts do not exist.

GREEN: create `scripts/phase7_local_preflight.ps1` as the single fail-closed local completion gate. It executes targeted Phase 7 tests; full collect-only with an explicit minimum equal to the committed baseline or higher; full pytest using ignored `.pytest-tmp/phase7-full-suite`; compileall; pip check; all Phase 7 Bash/PowerShell parse checks; `git diff --check`; a fully clean worktree including non-ignored untracked files; protected v1 archive/model/MPC and Phase 6 result checks; and canonical Phase 7 evidence absence. It exits nonzero on the first failed gate and prints exact counts/statuses. `phase7_prepare_bundle.ps1` must invoke this preflight itself and create no bundle if it fails, then bind the same tested HEAD to sidecar and complete bundle.

Create remaining scripts using new isolated paths `/root/EASYkoopman-phase7-v2` and `source/results/koopman_phase7`. Bootstrap clones to a non-existing target and verifies exact HEAD/clean tree. Server uses `set -Eeuo pipefail`, `PYTHONDONTWRITEBYTECODE=1`, `/root/IsaacLab/isaaclab.sh -p`, explicit Sim 5.0/Lab 2.2.1 preflight records, setuptools probe and offline editable install with `--no-deps --no-build-isolation --no-index`; it runs exactly three processes, captures full `PIPESTATUS`, validates each episode and logs before exact-three merge/hash. Pullback uses a fresh staging root, requires local clean HEAD equal sidecar before SCP, checks remote/local hashes plus source/Lab version/tag/commit sidecars, runs the tested validator and promotes only when canonical target is absent.

Write `docs/phase7_koopman_v2_runbook.md` with Evidence Levels, Local Preflight, Offline Bundle, Server Bootstrap, Server Three-Topology Smoke, Merge/Validate/Hash, Pullback, Failure Handling and Claim Boundary. State that the agent connects and runs commands directly after the user only powers on the server; do not instruct the user to execute server commands. Include exact commands and paths, and state explicitly that success proves schema/Bridge integration only, not Koopman effect.

After the dynamic script tests and native parse checks pass, create the atomic GREEN implementation commit for Task 2 before invoking the final preflight, because the preflight intentionally rejects any dirty tracked or unignored-untracked state. Make no further source/doc edits after that commit; if a correction is required, commit it and rerun the whole preflight. Run the complete local-preflight script at that clean committed HEAD, then prove bundle preparation calls the same script and succeeds only at the identical HEAD. Require canonical `source/results/koopman_phase7` absent.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\phase7_local_preflight.ps1</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Dynamic tests prove source/runtime/process/tee/semantic/SCP/hash/validator/promotion failures all stop before success evidence promotion.</criterion>
    <criterion>All scripts parse with their native shell; server commands use `isaaclab.sh -p` and offline install flags.</criterion>
    <criterion>Runbook contains exact base/uuv6/uuv4 commands, minimum 8 transitions, direct-agent SSH responsibility and the Koopman-effect claim boundary.</criterion>
    <criterion>The single local-preflight command executes and passes targeted/full pytest, collect threshold, compileall, pip check, shell parse, clean-worktree, diff/protected and canonical-evidence-absence gates before bundle creation.</criterion>
    <criterion>Final preflight runs after the Task 2 GREEN implementation is committed, with clean HEAD equal to the commit later written into bundle sidecar.</criterion>
    <criterion>`phase7_prepare_bundle.ps1` invokes the same preflight and dynamic tests prove no bundle command runs after any preflight failure.</criterion>
    <criterion>Prepare produces a verified complete bundle and 40-hex sidecar from a clean tested HEAD, while canonical Phase 7 evidence remains absent.</criterion>
  </acceptance_criteria>
  <done>The locally tested code is packaged into a fail-closed offline server chain and is ready for the only external checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Confirm server power, then agent executes and pulls back three-topology evidence</name>
  <files>source/results/koopman_phase7/, .planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-SERVER-EVIDENCE.md</files>
  <read_first>
    <file>docs/phase7_koopman_v2_runbook.md</file>
    <file>scripts/phase7_prepare_bundle.ps1</file>
    <file>scripts/phase7_server_bootstrap.sh</file>
    <file>scripts/phase7_server_smoke.sh</file>
    <file>scripts/phase7_pullback.ps1</file>
  </read_first>
  <action>
Pause only after Task 2 has produced a fully tested bundle and report that local code is server-ready. Ask the user only to ensure the unchanged server is powered on if SSH preflight is not already reachable; do not ask the user to run commands or change the server installation.

After confirmation/reachability, the executor directly uses SCP/SSH to transfer the bundle and commit sidecar, runs the Phase 7 bootstrap and `phase7_server_smoke.sh`, and retrieves all success or failure logs/rows. Run `phase7_pullback.ps1` to stage, compare hashes/sidecars and strict-validate before canonical promotion. Require `base`, `uuv6`, `uuv4` each to contain at least 8 contiguous rows; `uuv4` must contain a nonzero raw-yaw row with zero virtual yaw. Record actual Isaac Sim/Lab/Python versions, exact source commit, native/tee/semantic statuses, per-file/log hashes and aggregate hash. Preserve partial/failure evidence outside canonical success location and keep the checkpoint open if any gate fails.

Only after successful pullback create `07-SERVER-EVIDENCE.md` with exact commands/date/paths/hash/version/commit, a three-row topology table, semantic evidence for actual wrench/context and a CONT-01..05 plus D-04..16 evidence index. Write `phase7_gate: pass` only when every required item exists; otherwise write fail/incomplete and do not mark Phase 7 complete. Commit `source/results` evidence separately from later planning closure.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe workflows\validate_koopman_v2.py --aggregate source\results\koopman_phase7\evidence.json --json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>SSH execution uses the unchanged Isaac installation and isolated `/root/EASYkoopman-phase7-v2` checkout at the exact tested commit.</criterion>
    <criterion>All three unique topology episodes exist, each has at least 8 contiguous strict-valid transitions and exact runtime/log/hash provenance.</criterion>
    <criterion>The uuv4 artifact proves nonzero raw yaw and zero virtual yaw; all rows contain finite bounded PWM/control, actual thruster-only wrench and separated contexts.</criterion>
    <criterion>Staged pullback hash/source/runtime/validator checks pass before canonical promotion; local canonical evidence did not exist beforehand.</criterion>
    <criterion>`07-SERVER-EVIDENCE.md` contains `phase7_gate: pass` and concrete CONT-01..05 evidence; otherwise this checkpoint remains blocking.</criterion>
  </acceptance_criteria>
  <done>Actual server artifacts prove schema v2 and Bridge operation across 8/6/4 topology representatives and are independently pulled back and validated.</done>
</task>

</tasks>

<verification>
1. Runner/merge and server-chain tests pass locally without Isaac and retain only local_contract evidence.
2. Full repository regression, compileall, pip check, script parse, protected diff and clean bundle gates pass before SSH.
3. Server preflight proves exact source commit, Isaac Sim 5.0 and Isaac Lab 2.2.1; exact three processes/logs/native+tee+semantic gates pass.
4. `base/uuv6/uuv4` each provide at least 8 contiguous strict-valid transitions; uuv4 proves raw yaw→masked virtual yaw.
5. Pullback validates source/runtime sidecars, every SHA-256 and strict aggregate before atomic canonical promotion.
6. `07-SERVER-EVIDENCE.md`, then GSD SUMMARY/VERIFICATION, map CONT-01..05 without claiming Phase 8 Koopman performance or Phase 9 MPC integration.
</verification>

<success_criteria>
- Local code is completely tested before the user is asked to power/confirm the server.
- Real Isaac evidence covers all three actuator topologies and the critical underactuated-yaw semantic.
- Source, runtime, process, log, semantic, hash and validator provenance are fail closed end to end.
- v1 and Phase 6 evidence remain isolated and unchanged.
- Phase 7 claims only schema/Bridge integration; model effectiveness remains unproven until Phase 8.
- No HIGH threat remains unresolved.
</success_criteria>

<output>
After checkpoint success, create `.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-04-SUMMARY.md` with implementation/evidence commits, local test counts, bundle/source/runtime data, three-topology artifact/log hashes, pullback/validator results, deviations and threat dispositions. Then run the independent phase verifier to create `07-VERIFICATION.md`; update ROADMAP/STATE only if CONT-01..05 all pass. If server evidence is absent or fails, write an incomplete summary and keep Phase 7 open.
</output>
