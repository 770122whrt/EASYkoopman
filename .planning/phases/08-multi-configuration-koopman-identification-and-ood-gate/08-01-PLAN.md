---
phase: 08-multi-configuration-koopman-identification-and-ood-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - koopman/evidence_v2.py
  - koopman/protocol_v2.py
  - protocols/phase8/pilot_collection_policy.json
  - workflows/collect_koopman_v2_identification.py
  - workflows/audit_koopman_v2_pilot.py
  - workflows/validate_phase8_evidence.py
  - scripts/phase8_pilot_local_preflight.ps1
  - scripts/phase8_pilot_prepare_bundle.ps1
  - scripts/phase8_pilot_server_bootstrap.sh
  - scripts/phase8_pilot_server_run.sh
  - scripts/phase8_pilot_pullback.ps1
  - tests/test_phase8_protocol_evidence.py
  - tests/test_phase8_pilot_server_contract.py
  - docs/phase8_koopman_identification_runbook.md
  - source/results/koopman_phase8_pilot/
  - .planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-PILOT-EVIDENCE.md
autonomous: false
requirements: [KID-01, KID-05]
decisions: [D-01, D-09, D-10, D-11, D-12, D-18, D-19, D-20, D-21, D-22]
user_setup:
  - "After every local gate and the offline bundle pass, keep the unchanged Isaac Sim 5.0 / Isaac Lab 2.2.1 server powered on and SSH-reachable; the agent performs all SCP/SSH/server commands."
  - "Do not change `/root/IsaacLab`; the pilot uses the isolated target `/root/EASYkoopman-phase8-pilot-v2`."
must_haves:
  truths:
    - "D-11: the exact-eight pilot proves collection health only and cannot import, fit or evaluate a Koopman/baseline model"
    - "D-19: Phase 8 qualification is earned by an external envelope validator while every transition retains the frozen Phase 7 origin evidence value"
    - "D-12: the committed pilot policy hash predates every pilot episode, and actual inventory/hash facts are written only after collection"
    - "D-21/D-22: one clean fully tested commit is bundled before SSH; real pilot evidence is pulled back and committed separately from implementation"
    - "D-18: any missing/failed configuration, policy/source/runtime/log/hash drift or forbidden modeling output yields pilot_insufficient and keeps the checkpoint open"
  artifacts:
    - path: koopman/evidence_v2.py
      provides: additive exact Phase 8 external evidence envelope and strict validator
      contains: "PHASE8_EVIDENCE_ENVELOPE_V1"
    - path: protocols/phase8/pilot_collection_policy.json
      provides: pre-registered exact-eight 2x128 collection-health matrix
      contains: "phase8-pilot-collection-policy-v1"
    - path: workflows/audit_koopman_v2_pilot.py
      provides: model-free exact-eight pilot health and inventory audit CLI
      contains: "pilot_modeling_forbidden"
    - path: scripts/phase8_pilot_pullback.ps1
      provides: staged SCP/hash/source/runtime/envelope validation before canonical promotion
      contains: "source/results/koopman_phase8_pilot"
    - path: .planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-PILOT-EVIDENCE.md
      provides: real-server pilot command/hash/configuration evidence and allowed claim boundary
      contains: "pilot_health_gate"
  key_links:
    - from: workflows/collect_koopman_v2_identification.py
      to: workflows/koopman_bridge_v2.py
      via: one Bridge step per bounded policy action and one strict episode artifact per policy entry
      pattern: "KoopmanBridgeV2"
    - from: workflows/audit_koopman_v2_pilot.py
      to: koopman/evidence_v2.py
      via: external envelope is granted only after exact policy/inventory/runtime/log/semantic revalidation
      pattern: "validate_phase8_evidence"
---

<objective>
建立不漂移 Phase 7 transition schema 的 Phase 8 evidence/protocol 层，并在本地完整门通过后，于真实 Isaac 服务器执行 exact-eight collection-health pilot。

Purpose: 先证明八构型都能按预注册 bounded excitation 产生完整、多 episode、可哈希回收的数据，同时禁止把 pilot 当成识别/泛化结果。
Output: additive evidence envelope、pilot policy/collector/health auditor、完整离线服务器链、真实 `source/results/koopman_phase8_pilot` 和 `08-PILOT-EVIDENCE.md`。
</objective>

<execution_context>
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-SPEC.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-CONTEXT.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-RESEARCH.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-PATTERNS.md
</execution_context>

<context>
@koopman/schema_v2.py
@koopman/dataset_v2.py
@workflows/collect_koopman_v2_smoke.py
@workflows/merge_koopman_v2_evidence.py
@workflows/validate_koopman_v2.py
@scripts/phase7_local_preflight.ps1
@scripts/phase7_prepare_bundle.ps1
@scripts/phase7_server_bootstrap.sh
@scripts/phase7_server_smoke.sh
@scripts/phase7_pullback.ps1
@tests/test_phase7_server_evidence_contract.py
</context>

<threat_model>
  <assets>
    <asset>Frozen Phase 7 transition schema/evidence semantics</asset>
    <asset>Pre-registered pilot policy and real exact-eight episode/log inventory</asset>
    <asset>Tested source commit, unchanged Isaac runtime and canonical local pilot evidence</asset>
  </assets>
  <trust_boundaries>
    <boundary>Local policy/fixture claims to external Phase 8 qualification</boundary>
    <boundary>Clean tested checkout to offline bundle and isolated server checkout</boundary>
    <boundary>Server rows/logs/inventory to staged local canonical promotion</boundary>
  </trust_boundaries>
  <threats>
    <threat severity="HIGH">A new Phase 8 qualification string is inserted into the old row evidence field or a local envelope self-asserts real-server qualification.</threat>
    <threat severity="HIGH">Pilot code imports fitting/evaluation modules or uses all-eight trajectory behavior to choose a model feature, horizon, backend or threshold.</threat>
    <threat severity="HIGH">One configuration/episode/process/log fails but merger omits it, substitutes stale output or still reports pilot pass.</threat>
    <threat severity="HIGH">Executed server commit/runtime differs from the locally tested bundle or pullback validates with dirty/different code.</threat>
    <threat severity="MEDIUM">Policy-controlled paths/seeds/configurations escape roots, collide or overwrite canonical evidence.</threat>
  </threats>
  <mitigations>
    <mitigation>Separate immutable artifact-origin and qualification-level fields; only external validator may grant non-local qualification.</mitigation>
    <mitigation>Pilot auditor has an import allowlist and rejects model/fit/rank/condition/error/rollout fields and modeling module imports.</mitigation>
    <mitigation>Exact policy-entry inventory, one process status plus tee/semantic/log hashes per episode, no omission and atomic aggregate.</mitigation>
    <mitigation>Reuse Phase 7 clean HEAD/bundle/sidecar/isolated checkout/runtime/pullback gates with new non-overlapping roots.</mitigation>
    <mitigation>Exact choices, confined relative paths, symlink rejection, fresh targets and no overwrite.</mitigation>
  </mitigations>
  <blocked_on severity="HIGH">Any unresolved HIGH threat, absent server, failed exact-eight policy entry or source/runtime/hash disagreement keeps 08-01 incomplete; do not synthesize or downscope pilot evidence.</blocked_on>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN additive evidence envelope, pilot policy and model-free health audit</name>
  <files>tests/test_phase8_protocol_evidence.py, koopman/evidence_v2.py, koopman/protocol_v2.py, protocols/phase8/pilot_collection_policy.json, workflows/audit_koopman_v2_pilot.py, workflows/validate_phase8_evidence.py</files>
  <read_first>
    <file>.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-SPEC.md</file>
    <file>koopman/schema_v2.py</file>
    <file>workflows/merge_koopman_v2_evidence.py</file>
    <file>workflows/validate_koopman_v2.py</file>
    <file>tests/test_koopman_schema_v2.py</file>
  </read_first>
  <action>
RED first: create tests for exact envelope/policy schemas, duplicate/missing/extra keys, bounded JSON, 40/64-hex hashes, exact artifact-origin versus qualification meaning, qualification ordering, local self-promotion, stale protocol/source/runtime, unexpected files and atomic output. Assert old schema-v2 rows still accept only `local_contract`/`server_isaac_smoke` and reject all Phase 8 qualification strings. Commit the failing tests before production symbols exist.

Add an exact committed pilot policy with eight public configurations, two entries each: `axis_pulse` seed 8101 and `bounded_multisine` seed 8102, 128 transitions, raw magnitude <=0.25, unique episode IDs, expected task/controller/runtime and no model fields. RED health tests require exact 16 entries, complete episode/manifest/log hashes, finite/bounded schema rows, positive/negative coverage for each controllable virtual channel, nonzero raw-yaw probes with zero virtual yaw for `uuv4*`, exact topology/runtime and `collection_chain_ready` only. Add forbidden-output/import tests for fit/model/backend/feature/horizon/rank/condition/prediction/rollout/error artifacts or `koopman.model*`, `koopman.edmd*`, `koopman.evaluation*`, `koopman.loco_v2` imports.

GREEN: implement pure-Python exact schemas and deterministic reason codes in `koopman/evidence_v2.py` and `koopman/protocol_v2.py`. Implement model-free pilot audit and validator CLIs; re-read every referenced Phase 7 episode/manifest and log byte, never trust envelope hashes. Local builders can emit only local_contract. Preserve old schema code unchanged except imports/tests needed to prove compatibility. Write atomically and refuse existing canonical outputs.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_phase8_protocol_evidence.py tests/test_koopman_schema_v2.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>RED fails for missing Phase 8 symbols/behavior, followed by a separate GREEN commit with all target tests passing.</criterion>
    <criterion>Pilot policy has exactly 16 unique pre-registered entries and no post-collection hash/model result fields.</criterion>
    <criterion>Every Phase 8 qualification string is rejected as a transition evidence value but legal only in a validated external envelope.</criterion>
    <criterion>Pilot health audit cannot import/call modeling code and rejects any modeling-derived output field with `pilot_modeling_forbidden`.</criterion>
    <criterion>Missing/extra/stale episode/log/hash/runtime or uuv4 yaw-mask failure returns a stable nonzero reason and no pass envelope.</criterion>
  </acceptance_criteria>
  <done>The additive protocol/evidence vocabulary is strict, Phase 7-compatible and incapable of turning pilot into model evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN exact-policy collector and fail-closed pilot server chain</name>
  <files>tests/test_phase8_pilot_server_contract.py, workflows/collect_koopman_v2_identification.py, scripts/phase8_pilot_local_preflight.ps1, scripts/phase8_pilot_prepare_bundle.ps1, scripts/phase8_pilot_server_bootstrap.sh, scripts/phase8_pilot_server_run.sh, scripts/phase8_pilot_pullback.ps1, docs/phase8_koopman_identification_runbook.md</files>
  <read_first>
    <file>workflows/collect_koopman_v2_smoke.py</file>
    <file>workflows/koopman_bridge_v2.py</file>
    <file>scripts/phase7_local_preflight.ps1</file>
    <file>scripts/phase7_prepare_bundle.ps1</file>
    <file>scripts/phase7_server_smoke.sh</file>
    <file>scripts/phase7_pullback.ps1</file>
    <file>tests/test_phase7_server_evidence_contract.py</file>
  </read_first>
  <action>
RED first: add parser/action/runner tests for exact policy-driven entries, deterministic signed pulse/multisine values, no arbitrary CLI configuration/role mutation, minimum/exact transition count, unique fresh episode outputs, reset between episodes, AppLauncher-before-Gym/Torch/env, one Bridge call per action, and failure-before-eligible-finalize. Add mutation tests for policy escape/symlink/collision, duplicate/stale output, missing telemetry, wrong mask/topology/source/runtime, partial 16-entry merge and any modeling artifact.

Add dynamic PowerShell/Bash temp-Git tests for the full operational chain: targeted/full pytest and collect baseline, compileall, pip check, native shell parse, clean tracked+untracked tree, protected v1/Phase6/7 paths, canonical pilot absence, bundle completeness/sidecar, existing server root, exact server HEAD, Sim/Lab/setuptools/offline install, native+tee+semantic status, exact inventory, SCP/hash/local-validator drift and atomic promotion. RED must fail because Phase 8 scripts/runner do not exist.

GREEN: generalize the Phase 7 runner into a separate Phase 8 identification collector driven only by a validated policy entry. Keep Phase 7 collector unchanged. One server process per configuration may execute its two episodes sequentially, but each reset/logger/manifest is independent and any failure blocks the configuration and final exact-16 aggregate. Use `/root/IsaacLab/isaaclab.sh -p`, offline install flags, LF Bash, `set -Eeuo pipefail`, `PYTHONDONTWRITEBYTECODE=1`, full `PIPESTATUS`, isolated `/root/EASYkoopman-phase8-pilot-v2` and result root `source/results/koopman_phase8_pilot`.

Create the local preflight/prepare/bootstrap/server/pullback scripts and runbook sections `Evidence Vocabulary`, `Pilot Policy`, `Local Pilot Preflight`, `Offline Bundle`, `Server Exact-Eight Pilot`, `Pilot Health Audit`, `Pullback`, `Failure Handling`, `Main Protocol Checkpoint`, `Claim Boundary`. The runbook states that the agent performs SSH after the user only powers the server, and pilot success does not prove identification/OOD/MPC.

Commit GREEN before final clean preflight. Then run the single pilot local-preflight at the clean commit and prepare a verified bundle/sidecar from that exact HEAD. Do not create canonical pilot results locally.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\phase8_pilot_local_preflight.ps1</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Collector tests prove exact policy actions/episodes, independent resets/finalization and no model import/output.</criterion>
    <criterion>Dynamic operational tests fail closed for every source/runtime/process/tee/semantic/SCP/hash/validator/promotion mutation.</criterion>
    <criterion>Preflight runs targeted and full suite, compileall, pip check, native parse, clean/protected/canonical-absence gates and passes only at a committed clean HEAD.</criterion>
    <criterion>Verified offline bundle and 40-hex sidecar equal the tested HEAD; canonical pilot evidence remains absent.</criterion>
    <criterion>Runbook contains exact commands/paths, agent SSH responsibility and explicit collection-health-only claims.</criterion>
  </acceptance_criteria>
  <done>One fully tested clean commit is ready for direct-agent transfer to the unchanged server.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Confirm SSH reachability, then agent runs and pulls exact-eight pilot</name>
  <files>source/results/koopman_phase8_pilot/, .planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-PILOT-EVIDENCE.md</files>
  <read_first>
    <file>docs/phase8_koopman_identification_runbook.md</file>
    <file>protocols/phase8/pilot_collection_policy.json</file>
    <file>scripts/phase8_pilot_prepare_bundle.ps1</file>
    <file>scripts/phase8_pilot_server_bootstrap.sh</file>
    <file>scripts/phase8_pilot_server_run.sh</file>
    <file>scripts/phase8_pilot_pullback.ps1</file>
  </read_first>
  <action>
Pause only after Task 2 proves the local code and bundle are server-ready. If SSH is not reachable, ask the user only to power/confirm the unchanged server. The executor performs SCP/SSH, bootstraps the isolated exact commit, runs all 16 policy episodes, server-side health/envelope validation and inventory hash, then retrieves success or failure evidence.

Run staged pullback with exact local HEAD/sidecar, SCP native status, remote/local hashes, source and Isaac runtime sidecars, exact referenced-file inventory and tested local envelope validator. Promote only into a previously absent canonical pilot root. Partial/failure outputs remain outside canonical success and keep this checkpoint open.

After success, write `08-PILOT-EVIDENCE.md` with date/commands/paths/source/runtime, 8x2 episode table, row/log/hash inventory, channel/topology health, uuv4 yaw-mask evidence, `pilot_health_gate: pass`, allowed/disallowed claims and KID-01/KID-05 partial mapping. Commit real pilot evidence separately. Do not calculate rank, condition, prediction or rollout metrics.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe workflows\validate_phase8_evidence.py --envelope source\results\koopman_phase8_pilot\pilot_envelope.json --qualification server_isaac_identification_pilot --json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Exact tested source runs in isolated `/root/EASYkoopman-phase8-pilot-v2` with unchanged Isaac Sim 5.0/Lab 2.2.1.</criterion>
    <criterion>All 16 expected episodes exist with exactly 128 contiguous strict rows, logs and matching hashes; no extra/missing/stale file is tolerated.</criterion>
    <criterion>All controllable channels show predeclared signed coverage and both `uuv4*` variants prove nonzero raw yaw with zero virtual yaw.</criterion>
    <criterion>Envelope qualification is earned after external validation while every transition retains `server_isaac_smoke` origin.</criterion>
    <criterion>No model/rank/condition/error/rollout output exists; `08-PILOT-EVIDENCE.md` states collection-health-only claims.</criterion>
  </acceptance_criteria>
  <done>Real exact-eight pilot evidence proves the collection chain is ready; otherwise Phase 8 remains at pilot_insufficient.</done>
</task>

</tasks>

<verification>
1. Protocol/evidence mutation suite and Phase 7 schema regressions pass with no old evidence enum drift.
2. Pilot runner/server scripts pass local dynamic failure tests and one full clean preflight before bundle.
3. Real server output contains exact 8x2x128 policy entries, full logs/runtime/source/hash provenance and no modeling output.
4. Staged pullback independently revalidates every byte before canonical promotion.
5. `08-PILOT-EVIDENCE.md` closes only the collection-health checkpoint and does not claim KID model/OOD completion.
</verification>

<success_criteria>
- The Phase 8 qualification vocabulary is additive and old transition schema semantics remain frozen.
- Exact-eight collection health is real-server proven at one tested source/runtime.
- Pilot cannot influence feature/horizon/backend/model gates or masquerade as main data.
- Local implementation and pulled pilot evidence are isolated commits.
- No HIGH threat remains unresolved; otherwise the plan remains blocked with preserved failure evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-01-SUMMARY.md` with RED/GREEN commits, full preflight/bundle evidence, server commit/runtime, exact pilot inventory/hash, pullback result, claim limits, deviations and threat dispositions.
</output>
