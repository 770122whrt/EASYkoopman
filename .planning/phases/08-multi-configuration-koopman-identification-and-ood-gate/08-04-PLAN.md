---
phase: 08-multi-configuration-koopman-identification-and-ood-gate
plan: "04"
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - protocols/phase8/main_role_assignment_protocol.json
  - protocols/phase8/analysis_policy.json
  - workflows/collect_koopman_v2_identification.py
  - workflows/validate_phase8_evidence.py
  - scripts/phase8_main_local_preflight.ps1
  - scripts/phase8_main_prepare_bundle.ps1
  - scripts/phase8_main_server_bootstrap.sh
  - scripts/phase8_main_server_run.sh
  - scripts/phase8_main_pullback.ps1
  - tests/test_phase8_main_server_contract.py
  - docs/phase8_koopman_identification_runbook.md
  - source/results/koopman_phase8_dataset/
  - .planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-MAIN-DATA-EVIDENCE.md
autonomous: false
requirements: [KID-01, KID-02, KID-03, KID-05]
decisions: [D-09, D-10, D-12, D-14, D-16, D-18, D-19, D-20, D-21, D-22, D-23]
user_setup:
  - "At the D-23 checkpoint, review the exact generated main role/action and analysis-policy tables; approval freezes their hashes before server collection."
  - "After approval and a second clean local preflight/bundle, keep the unchanged server powered on and SSH-reachable; the agent performs all transfer and server commands."
  - "Keep `/root/IsaacLab` unchanged; main collection uses isolated `/root/EASYkoopman-phase8-main-v2`."
must_haves:
  truths:
    - "D-10/D-12: exact 8x12 episode roles/actions are committed and user-approved before collection; actual inventory/split hashes are created only after all episode bytes exist"
    - "D-14/D-16: one committed analysis policy freezes common candidate set, decision algorithm, horizons, SO(3) schema and gate template before any main fit/test"
    - "D-19: main dataset qualification is an external envelope over exact server-origin rows, not a rewritten transition evidence value"
    - "D-21/D-23: no SSH occurs until the user approves exact protocols and the approved commit passes full local preflight plus bundle verification"
    - "D-18/D-22: any missing/failed episode or provenance mismatch yields data_insufficient; implementation and real main dataset evidence remain separate commits"
  artifacts:
    - path: protocols/phase8/main_role_assignment_protocol.json
      provides: approved exact-eight 12x512 whole-episode role/action matrix
      contains: "phase8-main-role-protocol-v1"
    - path: protocols/phase8/analysis_policy.json
      provides: approved candidate/inner-decision/metric/gate template with forbidden inputs and seeds
      contains: "phase8-analysis-policy-v1"
    - path: scripts/phase8_main_server_run.sh
      provides: exact-96 episode server orchestration, validation, inventory/split/envelope and hashes
      contains: "koopman_phase8_dataset"
    - path: .planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-MAIN-DATA-EVIDENCE.md
      provides: real main collection inventory/provenance and KID data evidence mapping
      contains: "main_dataset_gate"
  key_links:
    - from: scripts/phase8_main_server_run.sh
      to: workflows/collect_koopman_v2_identification.py
      via: every committed role-protocol entry is collected exactly once with no CLI role override
      pattern: "main_role_assignment_protocol.json"
    - from: scripts/phase8_main_pullback.ps1
      to: workflows/validate_phase8_evidence.py
      via: staged main envelope/inventory/split and every referenced episode/log are revalidated before promotion
      pattern: "server_isaac_identification_dataset"
---

<objective>
将用户批准的 exact-eight main role/action 与 analysis policies 固定到一个完全测试的 clean commit，随后由 agent 在真实 Isaac 服务器采集、验证并拉回 Phase 8 主识别数据集。

Purpose: 提供 KID-01/02/03/05 后续 LOCO 可用的真实 immutable main bytes，同时保持 test roles、analysis choices 和证据等级在模型拟合前冻结。
Output: 两份 approved protocol、main operational chain、exact 96 episode server dataset/inventory/split/envelope 和 `08-MAIN-DATA-EVIDENCE.md`。
</objective>

<execution_context>
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-SPEC.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-CONTEXT.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-RESEARCH.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-PATTERNS.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-01-SUMMARY.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-02-SUMMARY.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-03-SUMMARY.md
</execution_context>

<context>
@protocols/phase8/pilot_collection_policy.json
@source/results/koopman_phase8_pilot/pilot_envelope.json
@koopman/protocol_v2.py
@koopman/collection_v2.py
@koopman/splits_v2.py
@koopman/metrics_v2.py
@workflows/collect_koopman_v2_identification.py
@scripts/phase8_pilot_local_preflight.ps1
@scripts/phase8_pilot_prepare_bundle.ps1
@scripts/phase8_pilot_server_run.sh
@scripts/phase8_pilot_pullback.ps1
</context>

<threat_model>
  <assets>
    <asset>User-approved pre-collection role/action and analysis policy hashes</asset>
    <asset>Exact 96 real main episodes/logs and post-collection inventory/split</asset>
    <asset>Tested source/runtime provenance and canonical local main dataset evidence</asset>
  </assets>
  <trust_boundaries>
    <boundary>Recommended protocol JSON to human-approved immutable experiment inputs</boundary>
    <boundary>Approved clean commit/bundle to isolated server collection</boundary>
    <boundary>Server main bytes/inventory/split/envelope to staged local promotion</boundary>
  </trust_boundaries>
  <threats>
    <threat severity="HIGH">Role, seed, scenario, length, action or analysis policy changes after approval/collection/test opening.</threat>
    <threat severity="HIGH">Pilot or Phase 7 rows are copied into main roles, or a failed main episode is patched/reassigned.</threat>
    <threat severity="HIGH">A partial exact-96 run is merged, an old file survives or post-collection inventory omits unexpected files.</threat>
    <threat severity="HIGH">Bundle/server/local validator commits or Isaac runtime differ while evidence is promoted.</threat>
    <threat severity="MEDIUM">Long server orchestration loses a process/tee log status or overwrites an earlier valid episode.</threat>
  </threats>
  <mitigations>
    <mitigation>Commit and hash exact protocols; D-23 approval precedes bundle; any requested change requires new experiment ID, recommit and full re-preflight.</mitigation>
    <mitigation>Disjoint pilot/main IDs/seeds/roots; exact fresh output per policy entry; failure never changes roles or creates eligible rows.</mitigation>
    <mitigation>Expected-set and actual-directory inventories must match exactly; every file/hash/log revalidated before aggregate.</mitigation>
    <mitigation>Clean HEAD/sidecar/bundle/server HEAD/source_commit/local HEAD equality plus exact runtime sidecars and tested validator.</mitigation>
    <mitigation>One isolated process per configuration, one independent episode logger/reset per entry, full native/tee/semantic status arrays and no overwrite.</mitigation>
  </mitigations>
  <blocked_on severity="HIGH">No protocol approval, failed local gate, unavailable server, partial dataset or provenance drift may be bypassed; preserve failure evidence and keep the plan open.</blocked_on>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN exact main protocols, runner orchestration and clean local gate</name>
  <files>tests/test_phase8_main_server_contract.py, protocols/phase8/main_role_assignment_protocol.json, protocols/phase8/analysis_policy.json, workflows/collect_koopman_v2_identification.py, scripts/phase8_main_local_preflight.ps1, scripts/phase8_main_prepare_bundle.ps1, scripts/phase8_main_server_bootstrap.sh, scripts/phase8_main_server_run.sh, scripts/phase8_main_pullback.ps1, docs/phase8_koopman_identification_runbook.md</files>
  <read_first>
    <file>koopman/protocol_v2.py</file>
    <file>koopman/collection_v2.py</file>
    <file>koopman/splits_v2.py</file>
    <file>koopman/loco_v2.py</file>
    <file>protocols/phase8/pilot_collection_policy.json</file>
    <file>scripts/phase8_pilot_server_run.sh</file>
    <file>tests/test_phase8_pilot_server_contract.py</file>
  </read_first>
  <action>
RED first: test an exact proposed main role protocol with the eight public configurations and, per configuration, 12 unique 512-transition episodes: for each of `independent_prbs`, `bounded_multisine`, `coupled_chirp`, fit seeds 8201/8202, validation seed 8301 and test seed 8401. All raw actions are deterministic and bounded <=0.25; policy/provenance IDs are disjoint from pilot. Require exactly six fit, three validation, three test per config and no runtime CLI override for role/seed/scenario/length/action family.

Test the exact proposed analysis policy: data prefixes `[2,4,6]`; observables `identity_v1,auv_kinematic_v1`; ridges `[1e-8,1e-6,1e-4,1e-2]`; declared normalization/platform candidates; horizons `[5,20,60,full]`; quaternion epsilon/projection rules; episode-block bootstrap alpha 0.05, fixed seed and resample count; equal-config metric/gate template; explicit forbidden inputs and eligible roles. Values are an approval proposal, not an optimality claim. Reject unknown fields, aliases, empty grids, nonfinite values and any post-test mutability flag.

Add runner/script dynamic failures for reset/finalize, wrong exact-96 set, pilot/main ID collision, role reassignment, extra/stale file, missing native/tee/semantic log, protocol/inventory timestamp inversion, source/runtime/sidecar drift, dirty code, incomplete bundle, SCP/hash/local-validator mismatch and promotion over canonical evidence. RED fails before main scripts/protocol files exist.

GREEN: extend only the Phase 8 collector to run each validated main policy entry; maintain independent logger/reset and fail the configuration on any entry. Implement main local-preflight/prepare/bootstrap/server/pullback by adapting the proven pilot chain to isolated `/root/EASYkoopman-phase8-main-v2` and `source/results/koopman_phase8_dataset`. Server must collect exact 96 entries, build actual inventory/split only after all bytes exist, validate external dataset envelope and write complete self-excluding file inventory/hash. Main preflight includes all Phase 8 targeted tests plus full repository regression, compileall, pip check, native parses, clean/protected/pilot-evidence-preservation and canonical-main/evaluation absence.

Update runbook with exact proposed tables, D-23 review instructions, main commands/time expectations, failure/resume rule (fresh experiment only; no partial append), and dataset-not-model claim. Commit GREEN, then run clean local preflight. Do not prepare/transfer the main bundle before Task 2 approval.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\phase8_main_local_preflight.ps1</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Protocol tests lock exact 8x12x512 roles/scenarios/seeds/actions and exact analysis-policy candidates/gates.</criterion>
    <criterion>Dynamic operational tests reject every partial/stale/dirty/runtime/hash/role/promotion failure.</criterion>
    <criterion>Full local preflight passes at a clean committed HEAD while canonical main/evaluation roots remain absent and pilot evidence is unchanged.</criterion>
    <criterion>No bundle or SSH side effect occurs before D-23 approval.</criterion>
    <criterion>Runbook labels every numerical policy value as pre-registered engineering design, not proven optimal.</criterion>
  </acceptance_criteria>
  <done>The exact proposed main experiment is locally executable and ready for explicit user review.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: User reviews and freezes the exact main collection and analysis policies</name>
  <files>protocols/phase8/main_role_assignment_protocol.json, protocols/phase8/analysis_policy.json</files>
  <read_first>
    <file>protocols/phase8/main_role_assignment_protocol.json</file>
    <file>protocols/phase8/analysis_policy.json</file>
    <file>source/results/koopman_phase8_pilot/pilot_health_decision.json</file>
    <file>docs/phase8_koopman_identification_runbook.md</file>
  </read_first>
  <action>
Present a concise table of exact per-configuration roles, episode counts/lengths, excitation families/seeds/action bounds, data prefixes, observables/ridges/normalization/platform candidates, horizons, SO(3)/projection rules, bootstrap settings, promotion template and forbidden inputs. Include the pilot collection-health result and explicitly state that pilot did not choose these values.

Ask the user to approve or request changes. On approval, record the two SHA-256 values and approved commit in the execution summary and proceed without editing. On requested change, stop before bundle/SSH, assign a new experiment ID, edit the protocol(s), rerun all Task 1 tests/full preflight, commit, and return to this checkpoint. Never reinterpret silence or prior general approval as D-23 protocol approval.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_phase8_protocol_evidence.py tests/test_phase8_main_server_contract.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>User has seen exact values and explicitly approved the current two file hashes.</criterion>
    <criterion>Approved hashes belong to the same clean commit that passed Task 1 preflight.</criterion>
    <criterion>No main bundle/server/canonical dataset exists before approval.</criterion>
  </acceptance_criteria>
  <done>The experiment protocol is frozen before any main collection or model fitting.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Prepare approved bundle, confirm SSH, then agent collects and pulls main dataset</name>
  <files>source/results/koopman_phase8_dataset/, .planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-MAIN-DATA-EVIDENCE.md</files>
  <read_first>
    <file>protocols/phase8/main_role_assignment_protocol.json</file>
    <file>protocols/phase8/analysis_policy.json</file>
    <file>scripts/phase8_main_prepare_bundle.ps1</file>
    <file>scripts/phase8_main_server_bootstrap.sh</file>
    <file>scripts/phase8_main_server_run.sh</file>
    <file>scripts/phase8_main_pullback.ps1</file>
  </read_first>
  <action>
After approval, rerun the full clean local preflight through `phase8_main_prepare_bundle.ps1`; require bundle/sidecar/protocol hashes all match the approved tested HEAD. If SSH is unreachable, ask the user only to power/confirm the unchanged server. The executor directly transfers, bootstraps and runs the isolated server chain.

Collect all 96 independent episodes. Any episode/configuration/process/log failure blocks inventory/envelope success; do not append a retry into the same experiment or reassign roles. Server creates actual-byte inventory, exact-eight split manifest and external `server_isaac_identification_dataset` envelope only after exact-set validation, then publishes an exact file inventory/hash.

Pull into fresh local staging; compare approved protocols, local/server HEAD/runtime, every episode/manifest/log/inventory/split/envelope hash and exact directory set; run the tested local validators before atomic promotion to an absent canonical root. Preserve failure evidence elsewhere.

Write `08-MAIN-DATA-EVIDENCE.md` only after pass: exact commands/date/paths/commit/runtime/protocol hashes, 8x12 role/count summary, total transitions, scenario/seed table, file inventory hash, split/hash result, pilot/main disjointness, allowed/disallowed claims and KID-01 plus data portions of KID-02/03/05. Commit server data separately from later evaluation.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe workflows\validate_phase8_evidence.py --envelope source\results\koopman_phase8_dataset\dataset_envelope.json --qualification server_isaac_identification_dataset --json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Approved/tested commit runs in isolated server root with unchanged Isaac runtime and exact protocol hashes.</criterion>
    <criterion>Exact eight configurations each contain six fit, three validation and three test episodes of exactly 512 strict rows: 96 episodes/49152 transitions total.</criterion>
    <criterion>Actual inventory/split are post-collection, exact, immutable and match all role/source/runtime/file hashes; pilot/main IDs and bytes are disjoint.</criterion>
    <criterion>Staged local validation passes before fresh canonical promotion; no partial/stale evidence is promoted.</criterion>
    <criterion>`08-MAIN-DATA-EVIDENCE.md` states dataset readiness only and does not claim model/OOD/MPC success.</criterion>
  </acceptance_criteria>
  <done>The approved exact-eight main identification dataset is real-server collected, independently pulled back and ready for one frozen LOCO evaluation.</done>
</task>

</tasks>

<verification>
1. Exact main role/analysis policies are test-locked, user-approved and hash-bound before bundle/collection.
2. Full local preflight and verified bundle run again after approval at the exact approved commit.
3. Server produces exact 96/49152 strict rows with complete native/tee/semantic/runtime/log/hash evidence.
4. Inventory/split are created from actual bytes after collection and revalidated during staged pullback.
5. Main evidence commit remains separate and claims data readiness only.
</verification>

<success_criteria>
- KID-01 has a real exact-eight complete-episode dataset with immutable roles and provenance.
- Analysis inputs/gates are frozen before any model fit/test and visible to the user.
- Pilot, main data, implementation and future evaluation remain isolated evidence levels/commits.
- Local code is completely tested before SSH and every server/pullback gate is fail closed.
- No HIGH threat remains unresolved; otherwise `data_insufficient` keeps Phase 8 open.
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-04-SUMMARY.md` with protocol approval/hashes, implementation commits, local preflight/bundle result, server/runtime/exact inventory evidence, pullback commit, KID data mapping, deviations and threat dispositions.
</output>
