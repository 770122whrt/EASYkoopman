---
phase: 08-multi-configuration-koopman-identification-and-ood-gate
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - koopman/preprocessing_v2.py
  - koopman/baselines_v2.py
  - koopman/model_v2.py
  - koopman/metrics_v2.py
  - koopman/loco_v2.py
  - koopman/protocol_v2.py
  - workflows/run_koopman_v2_loco.py
  - tests/test_koopman_model_v2.py
  - tests/test_koopman_metrics_v2.py
  - tests/test_koopman_loco_v2.py
  - tests/fixtures/koopman_phase8_synthetic_system/
autonomous: true
requirements: [KID-02, KID-03, KID-04]
decisions: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-13, D-14, D-16, D-17, D-18, D-20, D-22]
user_setup: []
must_haves:
  truths:
    - "D-02/D-03: one controlled-EDMD v2 backend serves per-config/pooled/conditional regimes; persistence/linear are baselines and expert is an evaluation namespace"
    - "D-04/D-05/D-06/D-08: eligible inputs are state_11 plus virtual_control_4 and optional physical platform descriptor; reference diagnostic and execution/environment diagnostics are non-promoting"
    - "D-09/D-14: every fold decision is produced by one pre-registered inner algorithm reading only seven source configurations"
    - "D-16/D-17: rollouts are episode-safe and official orientation/aggregate metrics are SO(3), per-config, equal-macro and worst-config"
    - "D-20: all v2 code is additive and old PWM_8 model/MPC/evaluation behavior remains unchanged"
  artifacts:
    - path: koopman/model_v2.py
      provides: versioned controlled-EDMD v2 backend, observable schemas, conditioning design and serialization
      contains: "ControlledEDMDV2"
    - path: koopman/metrics_v2.py
      provides: episode-safe one/multi/full rollout and quaternion-correct SO(3) metrics
      contains: "so3_geodesic_radians"
    - path: koopman/loco_v2.py
      provides: seven-source fold decisions and six-role result matrix without opening outer test during tuning
      contains: "fold_protocol_decision"
    - path: workflows/run_koopman_v2_loco.py
      provides: strict synthetic/real inventory CLI for fold decisions, fits and immutable results
      contains: "--analysis-policy"
  key_links:
    - from: koopman/loco_v2.py
      to: koopman/splits_v2.py
      via: consumes only validated primary/expert fold views and revalidates opened artifact audit
      pattern: "LOCOFoldManifestV2"
    - from: koopman/model_v2.py
      to: koopman/platform_features_v2.py
      via: conditional design uses a fold-fitted physical descriptor artifact, never configuration identity
      pattern: "PlatformFeatureNormalizerV2"
---

<objective>
用严格 TDD 实现一个 additive controlled-EDMD v2 backend、v2 baselines、episode-safe SO(3) evaluation 和七-source nested fold decision，使六个“角色”可比较但不会混成六种算法或泄漏 held-out trajectory。

Purpose: 完成 KID-02..04 的纯本地可验证核心，为真实 main dataset 到来后的一次性 LOCO evaluation 提供冻结接口。
Output: v2 preprocessing/model/baselines/metrics/LOCO runtime、synthetic known-system evidence 和完整 negative mutation matrix。
</objective>

<execution_context>
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-SPEC.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-CONTEXT.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-RESEARCH.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-PATTERNS.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-02-SUMMARY.md
</execution_context>

<context>
@koopman/model.py
@koopman/edmd.py
@koopman/baselines.py
@koopman/lifting.py
@koopman/evaluation.py
@koopman/sweep.py
@koopman/collection_v2.py
@koopman/splits_v2.py
@koopman/platform_features_v2.py
</context>

<threat_model>
  <assets>
    <asset>Controlled-EDMD v2 input/observable/serialization meaning</asset>
    <asset>Held-out fold decision isolation and role eligibility</asset>
    <asset>Quaternion-correct rollout and configuration-balanced metrics</asset>
  </assets>
  <trust_boundaries>
    <boundary>Validated episode arrays to model preprocessing/design matrices</boundary>
    <boundary>Seven-source fold view to selected hyperparameters/normalizers</boundary>
    <boundary>Model predictions to finite quaternion-safe evaluation/aggregate</boundary>
  </trust_boundaries>
  <threats>
    <threat severity="HIGH">v1 reference/PWM semantics or a diagnostic field silently enters the eligible v2 design.</threat>
    <threat severity="HIGH">Conditional interactions memorize configuration identity or use a normalizer fitted with held-out facts.</threat>
    <threat severity="HIGH">Outer-test rows/metrics influence data prefix, observables, ridge, features, thresholds or a refit.</threat>
    <threat severity="HIGH">Teacher forcing, cross-episode windows or quaternion component RMSE produce a false long-horizon/orientation claim.</threat>
    <threat severity="MEDIUM">Expert/reference diagnostic is incorrectly marked eligible or feeds the primary decision.</threat>
  </threats>
  <mitigations>
    <mitigation>Exact input schema and function signatures with named diagnostics outside the design builder.</mitigation>
    <mitigation>Versioned physical descriptor artifact and dynamic source-set validation before every conditional fit/predict.</mitigation>
    <mitigation>Fold decision consumes recording seven-source views and is sealed/hash-written before held-out test loader is callable.</mitigation>
    <mitigation>Explicit episode iteration, one true initial state, predicted-state recursion, quaternion validation/projection and SO(3) property tests.</mitigation>
    <mitigation>Eligibility is an enum derived from role, not caller-set bool; expert/diagnostic namespaces are rejected by selector-facing APIs.</mitigation>
  </mitigations>
  <blocked_on severity="HIGH">Any forbidden input, held-out read, invalid rollout/metric alias or role promotion passing tests blocks the plan.</blocked_on>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN quaternion preprocessing, v2 baselines and one controlled-EDMD backend</name>
  <files>tests/test_koopman_model_v2.py, tests/fixtures/koopman_phase8_synthetic_system/, koopman/preprocessing_v2.py, koopman/baselines_v2.py, koopman/model_v2.py</files>
  <read_first>
    <file>koopman/model.py</file>
    <file>koopman/edmd.py</file>
    <file>koopman/baselines.py</file>
    <file>koopman/dataset_v2.py</file>
    <file>koopman/platform_features_v2.py</file>
    <file>.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-RESEARCH.md</file>
  </read_first>
  <action>
RED first: build deterministic multi-config controlled-system fixtures with known next-state mapping, including quaternion sign flips and one physical parameter effect. Test `PersistenceBaselineV2` and `SimpleLinearBaselineV2` exact input widths/no reference. Define exact observable schemas: `identity_v1` has bias+11 state features; `auv_kinematic_v1` has exact 40 ordered features (bias, state11, depth square, three linear-velocity squares, three angular-velocity squares, 12 quaternion×angular-velocity products, nine linear×angular-velocity products). Assert no reference/PWM/wrench/environment/config identity key can enter.

Test one `ControlledEDMDV2` with `conditioning=none|platform_affine`, ridge>=0, finite/rank diagnostics, deterministic solve/pinv behavior, batch/single predict, 4D control, 11D target, exact model/preprocessor/normalizer hashes and save/load round trip. Conditional design must contain `[phi,u,p,kron(p,[phi,u])]`; pooled/per-config contain no `p`. Add model failures for wrong fold/source normalizer, held-out config, nonfinite, shape drift, stale schema/hash and v1 model loading. Commit RED before v2 modules exist.

GREEN: implement episode-continuous quaternion sign alignment without rewriting stored rows; deterministic first-row rule and next-state alignment; version/hash it. Implement v2 baselines and controlled EDMD with exact observable constants, source-only normalizers, immutable metadata and atomic model serialization. `identity_v1` is a sanity candidate that may equal simple linear; do not claim it is a nonlinear win. Keep all existing v1 files unchanged.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_model_v2.py tests/test_koopman_platform_features_v2.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Known-system fixtures recover/predict the expected controlled mapping within declared numerical tolerance.</criterion>
    <criterion>Observable dimensions/order and conditional Kronecker design are exact, versioned and round-trip stable.</criterion>
    <criterion>Eligible fit/predict signatures contain no reference/PWM/wrench/environment/config identity route.</criterion>
    <criterion>Quaternion sign preprocessing is episode-continuous, non-mutating and provenance-hashed.</criterion>
    <criterion>Old v1 model/EDMD/baseline/PWM_8 tests and protected diffs remain unchanged.</criterion>
  </acceptance_criteria>
  <done>One additive v2 backend and two true v2 baselines operate on the frozen 11D/4D contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN episode-safe rollout, SO(3) and configuration-balanced metrics</name>
  <files>tests/test_koopman_metrics_v2.py, koopman/metrics_v2.py</files>
  <read_first>
    <file>koopman/evaluation.py</file>
    <file>koopman/model_v2.py</file>
    <file>koopman/collection_v2.py</file>
    <file>koopman/splits_v2.py</file>
    <file>.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-SPEC.md</file>
  </read_first>
  <action>
RED first: add SO(3) property tests for q/-q=0, 0/90/180 degree radians, dot clamp, batch shape, nonunit valid projection, zero norm, NaN/Inf and projection-limit divergence. Test state group slices exactly depth 0, quaternion 1:5, linear velocity 5:8, angular velocity 8:11.

Add two-episode models proving one-step uses each row, multi-step windows never cross episode, full rollout starts at one true state and recursively consumes predictions plus recorded `virtual_control_4`, and any intermediate truth reset is detected. Require pre-registered 5/20/60 plus full metrics when episode length permits, explicit short-episode failure otherwise, per-episode/per-config counts, equal-config macro, worst-config and diagnostic-only row-weighted aggregate. Reject attitude/component aliases, missing configs/horizons, nonfinite silent drop and uuv4 reference/yaw-target scoring fields.

GREEN: implement pure NumPy metric functions, immutable metric schema/version, quaternion projection diagnostics and reason-coded rollout results. Divergence thresholds come only from the passed analysis/fold policy, not module defaults. Every failure remains a result entry; no silent row/episode deletion. Aggregate recomputes from per-config artifacts and refuses caller-supplied summary values.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_metrics_v2.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Official orientation fields derive only from sign-invariant normalized SO(3) radians.</criterion>
    <criterion>Open-loop predictions never teacher-force or cross episode boundaries.</criterion>
    <criterion>Each configuration receives equal macro weight regardless of row/episode count and worst-config is mandatory.</criterion>
    <criterion>Nonfinite/invalid/projection/divergence events are counted and reason-coded, never dropped or silently clipped.</criterion>
  </acceptance_criteria>
  <done>KID-03/04 metrics are correct, episode-safe and cannot hide a weak configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED→GREEN seven-source fold decisions, six roles and non-promoting diagnostics</name>
  <files>tests/test_koopman_loco_v2.py, koopman/protocol_v2.py, koopman/loco_v2.py, workflows/run_koopman_v2_loco.py</files>
  <read_first>
    <file>koopman/splits_v2.py</file>
    <file>koopman/model_v2.py</file>
    <file>koopman/baselines_v2.py</file>
    <file>koopman/metrics_v2.py</file>
    <file>koopman/sweep.py</file>
    <file>koopman/selection.py</file>
  </read_first>
  <action>
RED first: define exact `phase8-analysis-policy-v1` schema with allowed data prefixes `{2,4,6}`, observables `{identity_v1,auv_kinematic_v1}`, a finite ridge grid, declared normalization/platform candidates, horizons `{5,20,60,full}`, deterministic lexicographic/paired source-validation decision algorithm, bootstrap alpha/seed, metric/gate template and explicit forbidden inputs/roles. This is a schema/fixture in 08-03; the canonical user-approved policy is written in 08-04.

Instrument every dataset/statistic access. For each fold, require the decision to list exactly seven source configurations and their fit/validation hashes, selected data prefix/observable/ridge/normalization/platform schema, rank/condition/convergence diagnostics and numeric gates. It must be atomically sealed before a held-out test view can open. Mutations for held-out read/statistic, post-test retune, grid expansion, different budget, stale normalizer or missing decision return stable nonzero reasons.

Test the exact six roles: persistence, simple-linear, source per-configuration models, pooled, conditional and held-out expert. Pooled/conditional alone are eligible; source per-config explains ID learnability; expert fits held-out fit, tunes on held-out validation and shares held-out test only after primary decision seal. Add optional `reference_conditioned_diagnostic_v2` with separate namespace and hard `selection_eligible=false`; prove its metrics/choices never affect primary decisions. Every fold/role gets success or reason-coded failure.

GREEN: implement deterministic fold decision and role orchestration using only the validated view types. Separate decision, fit and test-open methods so call order is enforceable. The CLI accepts inventory/split/analysis-policy/output root, supports a single fold or all folds, confines fresh outputs and writes full hashes. It must not implement final selection yet.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_loco_v2.py tests/test_koopman_model_v2.py tests/test_koopman_metrics_v2.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Eight synthetic folds produce decisions from exact seven-source opened hashes before any held-out test open.</criterion>
    <criterion>All six roles are present or reason-coded fail; only pooled/conditional expose eligible result types.</criterion>
    <criterion>Expert and reference diagnostic cannot feed primary decision/selection paths by type, field or mutation.</criterion>
    <criterion>Post-test grid/gate/refit mutation, held-out statistic access and stale fold artifacts fail closed.</criterion>
    <criterion>Synthetic pooled/conditional behavior is evaluated under identical split/budget/metric policy, not a different per-role protocol.</criterion>
  </acceptance_criteria>
  <done>The complete nested LOCO fitting/evaluation core is locally proven without real Phase 8 main data.</done>
</task>

</tasks>

<verification>
1. Model TDD proves exact dimensions, one backend, conditional physical interactions and v1 isolation.
2. SO(3) property tests and episode-safe rollout/aggregation tests pass.
3. Dynamic opened-file tests prove every fold decision uses seven source configurations only.
4. Six role results and eligibility are exact; expert/reference diagnostics never affect primary selection.
5. Targeted plus v1/Phase7 regressions, compileall and protected diffs pass; canonical main/evaluation roots remain absent.
</verification>

<success_criteria>
- KID-02 model-role semantics are implemented without algorithm-name inflation.
- KID-03 one/multi/full prediction and configuration-balanced aggregation are executable.
- KID-04 official SO(3) semantics and invalid quaternion handling are property-tested.
- Exact-eight nested tuning is held-out sealed and provenance-bound.
- No HIGH threat remains unresolved and no Phase 9 controller claim/code is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-03-SUMMARY.md` with RED/GREEN commits, exact observable/model/metric versions, synthetic-system results, fold access audits, role/eligibility matrix, v1 protected checks, deviations and threat dispositions.
</output>
