---
phase: 08-multi-configuration-koopman-identification-and-ood-gate
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - koopman/collection_v2.py
  - koopman/splits_v2.py
  - koopman/platform_features_v2.py
  - koopman/protocol_v2.py
  - workflows/build_koopman_v2_inventory.py
  - workflows/build_koopman_v2_splits.py
  - tests/test_koopman_collection_v2.py
  - tests/test_koopman_splits_v2.py
  - tests/test_koopman_platform_features_v2.py
  - tests/fixtures/koopman_phase8_multi_episode/
autonomous: true
requirements: [KID-01, KID-02]
decisions: [D-04, D-06, D-07, D-09, D-10, D-12, D-13, D-14, D-18, D-20, D-22]
user_setup: []
must_haves:
  truths:
    - "D-10/D-12: role intent is frozen before collection, while actual inventory and split hashes are created only from collected bytes before fitting"
    - "D-09: each of eight outer folds exposes exactly seven source configurations to primary decisions and seals every held-out trajectory"
    - "D-13: held-out fit/validation episodes are addressable only through an expert namespace and cannot enter primary loaders/statistics"
    - "D-07: physical descriptors contain no configuration identity and their normalizers record exact seven-source provenance"
    - "D-20/D-22: the additive collection/split layer leaves v1 and Phase 7 data contracts/evidence unchanged and creates no canonical Phase 8 success directory"
  artifacts:
    - path: koopman/collection_v2.py
      provides: immutable multi-episode schema-v2 collection with retained configuration/role/hash boundaries
      contains: "KoopmanDatasetCollectionV2"
    - path: koopman/splits_v2.py
      provides: exact-eight role and LOCO manifests plus opened-artifact leakage audit
      contains: "LOCOFoldManifestV2"
    - path: koopman/platform_features_v2.py
      provides: versioned physical-only platform descriptor candidates and source-fold normalizer
      contains: "configuration_identity_forbidden"
    - path: workflows/build_koopman_v2_inventory.py
      provides: strict post-collection actual-byte inventory builder/validator CLI
      contains: "dataset_inventory"
  key_links:
    - from: koopman/collection_v2.py
      to: koopman/dataset_v2.py
      via: validates each episode independently and retains immutable DatasetV2 objects instead of concatenating raw rows
      pattern: "load_koopman_episode_v2"
    - from: koopman/splits_v2.py
      to: koopman/collection_v2.py
      via: derives roles/folds from immutable inventory IDs and records every opened episode hash
      pattern: "inventory_sha256"
---

<objective>
建立采集意图、实际文件事实和模型 split 三者分离的多 episode 数据层，并用动态文件访问证据证明 exact-eight LOCO 没有 configuration/episode 泄漏。

Purpose: 在模型代码出现前先固定 KID-01 的可信边界，让后续任何 normalization、feature selection 或 fit 都无法误读 held-out configuration。
Output: immutable collection/inventory、role protocol、八个 LOCO fold manifests、physical-only descriptor/normalizer 和完整 leakage mutation tests。
</objective>

<execution_context>
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-SPEC.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-CONTEXT.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-RESEARCH.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-PATTERNS.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-01-SUMMARY.md
</execution_context>

<context>
@koopman/dataset_v2.py
@koopman/schema_v2.py
@koopman/v1_compatibility.py
@koopman/splits.py
@koopman/normalization.py
@easyuuv_nc/embodiments.py
@tests/test_koopman_dataset_v2.py
@tests/test_koopman_splits.py
</context>

<threat_model>
  <assets>
    <asset>Pre-collection role assignments and post-collection immutable episode bytes</asset>
    <asset>Exact-eight LOCO source/held-out sets and opened-artifact audit trail</asset>
    <asset>Physical platform descriptor meaning and source-only normalization</asset>
  </assets>
  <trust_boundaries>
    <boundary>Untrusted role protocol/inventory JSON to validated multi-episode objects</boundary>
    <boundary>Complete collection to one fold's permitted primary/expert views</boundary>
    <boundary>Configuration provenance to physical conditional features</boundary>
  </trust_boundaries>
  <threats>
    <threat severity="HIGH">Rows from one episode are split, duplicated or silently repaired across fit/validation/test.</threat>
    <threat severity="HIGH">Held-out fit/validation/test bytes or statistics are opened during primary fold decisions.</threat>
    <threat severity="HIGH">A post-collection role edit or stale inventory changes which files are considered test.</threat>
    <threat severity="HIGH">Configuration name/one-hot or held-out normalization leaks identity into conditional features.</threat>
    <threat severity="MEDIUM">Concatenation loses episode boundaries and later rollout crosses files.</threat>
  </threats>
  <mitigations>
    <mitigation>Exact role protocol hash, unique episode IDs, immutable inventory entries and whole-episode role validation.</mitigation>
    <mitigation>Fold-scoped loaders require an allowed configuration set and record every opened episode/hash/statistic.</mitigation>
    <mitigation>Recompute role/split sets from policy plus inventory; do not trust caller path lists.</mitigation>
    <mitigation>Versioned physical schema rejects identity fields; normalizer binds exact seven-source configuration and episode hashes.</mitigation>
    <mitigation>Collection stores episode objects and explicit indexes; no implicit flat array is the source of truth.</mitigation>
  </mitigations>
  <blocked_on severity="HIGH">Any leakage mutation, role/inventory drift or identity feature accepted by validators blocks completion.</blocked_on>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN immutable multi-episode collection and post-collection inventory</name>
  <files>tests/test_koopman_collection_v2.py, tests/fixtures/koopman_phase8_multi_episode/, koopman/collection_v2.py, workflows/build_koopman_v2_inventory.py</files>
  <read_first>
    <file>koopman/dataset_v2.py</file>
    <file>koopman/schema_v2.py</file>
    <file>koopman/evidence_v2.py</file>
    <file>tests/test_koopman_dataset_v2.py</file>
    <file>source/results/koopman_phase8_pilot/pilot_envelope.json</file>
  </read_first>
  <action>
RED first: create deterministic local-contract fixtures with at least three configurations, multiple episode IDs and all three roles; generation must use schema-v2 logger then delete any one-time generator. Add failing tests for exact episode/manifest validation, unique ID/config/role/scenario/seed, immutable episode objects and indexes, expected-versus-actual role comparison, duplicate/missing/extra/stale files, bad hash/record count/invariant, role mutation, Phase 7/pilot eligibility, path escape/symlink and bounded inventory. Commit RED before collection/inventory symbols exist.

GREEN: implement `KoopmanDatasetCollectionV2` as a tuple/index of independently validated `KoopmanDatasetV2` episodes, never a boundary-free source array. Add explicit APIs for selecting complete episodes by configuration/role and producing a row view only with returned boundary metadata. Implement `phase8-dataset-inventory-v1` from actual bytes after collection: relative path, JSONL/manifest hashes/sizes/counts/invariants, role-protocol hash, source/runtime/envelope links and canonical aggregate hash. The CLI validates first, writes `.part`, fsyncs and atomically promotes only to a fresh path.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_collection_v2.py tests/test_koopman_dataset_v2.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Fixture hashes are locked and every fixture remains local_contract/non-promoting.</criterion>
    <criterion>Collection preserves immutable episode boundaries, role/config indexes and exact source hashes.</criterion>
    <criterion>Inventory cannot be written before bytes exist and rejects desired-vs-actual set/hash/role differences.</criterion>
    <criterion>Phase 7 smoke and Phase 8 pilot envelopes cannot satisfy main dataset qualification.</criterion>
  </acceptance_criteria>
  <done>Actual episode facts can be loaded and inventoried without erasing roles or overstating evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN role protocol, exact-eight LOCO and dynamic access-leakage gates</name>
  <files>tests/test_koopman_splits_v2.py, koopman/protocol_v2.py, koopman/splits_v2.py, workflows/build_koopman_v2_splits.py</files>
  <read_first>
    <file>koopman/collection_v2.py</file>
    <file>koopman/splits.py</file>
    <file>koopman/protocol_v2.py</file>
    <file>.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-SPEC.md</file>
    <file>tests/test_koopman_splits.py</file>
  </read_first>
  <action>
RED first: add exact-eight synthetic inventory and tests for a recommended `phase8-main-role-protocol-v1`: twelve episodes/config, 512 transitions, six fit/three validation/three test, excitation families `independent_prbs`, `bounded_multisine`, `coupled_chirp`, role-separated seeds and unique IDs. Treat these as a pre-collection proposal requiring D-23 approval, not current server evidence. Add mutation failures for row-level split, episode overlap, role change after collection, config omission/duplicate/extra, unequal collection matrix, pilot/main ID reuse and inventory/protocol order/hash drift.

Require exactly eight `LOCOFoldManifestV2`: primary source sets exact seven; primary held-out test exact one config; held-out fit/validation absent from every primary view; expert view gets only its own held-out fit/validation plus the same held-out test; all fold IDs/configs occur once. Add a dynamic recording loader/fake filesystem that fails if primary fold-decision code opens any held-out episode, even when a forged manifest path claims it is source. Record opened hashes/configurations and validate them against exact allowed sets.

GREEN: implement exact schemas/builders/validators and deterministic CLIs. Split generation derives from the validated inventory and role protocol; it does not accept caller-supplied arbitrary path lists. Expose separate primary/expert view constructors with distinct types/namespaces. Every fold decision input manifest records source episode hashes and a sealed held-out digest without reading held-out bytes. Use atomic fresh writes.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_splits_v2.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Exactly eight folds cover each held-out configuration once and each primary source set is the complementary seven.</criterion>
    <criterion>No episode crosses role and no held-out fit/validation/test byte is opened during primary decision construction.</criterion>
    <criterion>Expert view is separate, contains only its held-out configuration and cannot be converted to a primary/eligible view.</criterion>
    <criterion>Role/hash/timestamp mutations and forged path lists fail with stable leakage or drift reasons.</criterion>
  </acceptance_criteria>
  <done>The exact-eight nested split is machine-enforced before any model code consumes it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED→GREEN physical platform feature candidates and v1/provenance isolation</name>
  <files>tests/test_koopman_platform_features_v2.py, koopman/platform_features_v2.py</files>
  <read_first>
    <file>koopman/schema_v2.py</file>
    <file>koopman/normalization.py</file>
    <file>easyuuv_nc/embodiments.py</file>
    <file>koopman/splits_v2.py</file>
    <file>koopman/model.py</file>
    <file>koopman/mpc.py</file>
  </read_first>
  <action>
RED first: add exact feature-schema tests for candidates built only from declared physical facts: positive mass/volume/inertia/drag/time constant transforms, COM/COB offset, normalized thruster count/rank, control mask and fixed allocation-mode descriptor. Reject configuration/name/one-hot/hash-derived identity, unknown categories, non-finite/invalid scale and per-row platform drift. Add leave-one-config tests proving feature normalizer fits only exact seven source configurations/episode hashes and rejects held-out or different-fold reuse.

GREEN: implement versioned descriptor builders returning immutable arrays plus schema/provenance artifacts. Keep candidate schema selection separate from feature extraction. Implement a source-fold `PlatformFeatureNormalizerV2` with exact config/episode/schema hashes and deterministic zero-scale behavior; no global singleton statistics. Add cold-import and protected v1/Phase6/7 diff tests. Do not modify old `StandardNormalizer`, v1 model/MPC or stored evidence.
  </action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_platform_features_v2.py tests/test_koopman_splits_v2.py tests/test_koopman_dataset_v2.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>No feature schema contains configuration identity, one-hot or values derived from held-out trajectories.</criterion>
    <criterion>Each normalizer binds exactly seven source configurations and the exact episode/schema hashes used to fit it.</criterion>
    <criterion>All eight configurations can be transformed when a schema is frozen; unknown/missing physical facts fail closed.</criterion>
    <criterion>Cold imports remain Isaac-free and v1 model/MPC plus Phase 6/7 evidence show no diff.</criterion>
  </acceptance_criteria>
  <done>The conditional regime has a physical-only, fold-local and provenance-auditable input path.</done>
</task>

</tasks>

<verification>
1. Multi-episode fixture/inventory tests pass without Isaac and retain exact boundaries/hashes.
2. Exact-eight folds and dynamic opened-file instrumentation prove held-out trajectories are sealed from primary decisions.
3. Expert and primary namespaces cannot be confused or promoted through ordinary APIs.
4. Physical descriptors/normalizers contain only source-fold facts and no configuration identity.
5. Compileall, diff-check, protected v1/Phase6/7 paths and full targeted regressions pass; no canonical Phase 8 main/evaluation evidence is created.
</verification>

<success_criteria>
- KID-01 has an executable role/inventory/split contract rather than a row-list convention.
- Every model-affecting consumer can receive only exact seven-source fold views.
- Conditional context is physical and fold-local, never an identity shortcut.
- v1 and Phase 7 data semantics remain unchanged.
- No HIGH threat or leakage mutation remains unresolved.
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-02-SUMMARY.md` with TDD commits, fixture hashes, collection/inventory schemas, exact fold sets, leakage test evidence, physical feature schemas, protected-path checks and threat dispositions.
</output>
