# Plan 08.2-02 Summary — Fresh Server Collection and Validated Pullback

## Outcome

Fresh v2.1 exact-eight server dataset collected on the unchanged Isaac server from the verified bundle, staged-pulled, validated and atomically promoted as the first canonical Phase 8.2 evidence root. The first `v1` bootstrap attempt failed operationally before any collection and is preserved as a failed attempt.

## Delivered

- **Failed `v1` attempt (preserved):** the first server bootstrap cloned the verified source but stopped before collection because the bootstrap script reassigned its own readonly `RESULT_ROOT` variable. Root `/root/EASYkoopman-phase8-2-v1` is preserved and was never reused. No episode, model, held-out test or formal result was created by it.
- **Fresh `v2` collection:** from tested source commit `4f3b4cb`, checkout `/root/EASYkoopman-phase8-2-v2`, results `/root/EASYkoopman-phase8-2-results-v2`. Eight configuration collectors completed 8 × 12 = 96 v2.1 episodes (six fit, three validation, three test per configuration), 512 transitions each — 96 episode JSONL, 96 manifests, 96 collector logs, zero `.part` files, 49,152 strict schema-v2.1 transitions in total.
- **Gates before indexing:** per-configuration native, tee and semantic statuses plus `collection_complete.status` were all written under `collection_status/` before the dataset index was built; any failure would have blocked inventory creation.
- **Staged pullback:** local pullback validated native/tee/semantic statuses, relative SHA-256 inventory, exact canonical protocol/approval hashes, all v2.1 artifacts and the exact-eight LOCO split in a random staging root, then atomically promoted `dataset/` and `collection_status/` into `source/results/koopman_phase8_2/`.
- **Committed evidence:** dataset and collection-status bytes are committed (dataset `fa2fda0`, evidence-bytes follow-up `460c80a`); Git text conversion is disabled for the immutable dataset subtree.

## Verification

| Gate | Result |
|---|---|
| Verified offline bundle + sidecars before SSH | PASS (Plan 08.2-01) |
| Bootstrap `v2` fresh-root refusal of existing roots | PASS |
| Native/tee/semantic gates, 8 configurations | PASS (8×3 status files + `collection_complete.status`) |
| Episode/manifest/log exact set (96/96/96, zero `.part`) | PASS |
| Staged pullback validation before atomic promotion | PASS |
| `validate_phase82_dataset_index.py` on promoted dataset | PASS — `{"episode_count":96,"validation_gate":"phase82_dataset_index_valid"}` |
| Clean tree + protocol drift check (formal preflight, Plan 08.2-03) | PASS |

## Evidence boundary

Collection-health and dataset readiness only. No model has been fitted, no held-out
evaluation, selection or closeout has run, and no claim about prediction quality,
OOD transfer, MPC, environment adaptation or Agentic behavior is supported by this
plan. Formal evaluation is Plan 08.2-03 work.
