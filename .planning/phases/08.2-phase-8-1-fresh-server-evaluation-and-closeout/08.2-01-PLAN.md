---
phase: 08.2-phase-8-1-fresh-server-evaluation-and-closeout
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [KIDO-01]
autonomous: true
---

# Plan 08.2-01 — Local Operational Readiness and Offline Bundle

## Objective

Create and test the complete Phase 8.2 operational shell, validate canonical D-23, commit a clean source HEAD and produce an offline-verified bundle. Stop before SSH.

## Tasks

1. Add RED tests for approval-bound script naming/paths, fresh-root guards, bundle verification, server result isolation, inventory/split builder and formal command ordering.
2. Implement the protocol inventory/split builder and the independent `phase8_2_*` scripts/runbook.
3. Run targeted Phase 8.2 tests, relevant Phase 8.1 regressions, script parsers, compileall and `git diff --check`.
4. Commit the implementation, rerun the checked-in local preflight from clean HEAD, create and verify the offline bundle by isolated clone, then record the tested commit and bundle hash.

## Done when

- canonical approval validation passes;
- operational scripts are tested and syntax-valid;
- HEAD is clean and committed;
- `.pytest-tmp/phase8-2-transfer/EasyUUV-phase8-2-v1.bundle` is verified and cloneable offline;
- no SSH, server result or formal output exists.
