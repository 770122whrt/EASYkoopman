# Phase 8.2 Context — Fresh Server Evaluation and Closeout

## Why this phase exists

Phase 8.1 repaired simulator and identification semantics locally. D-23 approved the exact role protocol and analysis policy, but no fresh v2.1 Isaac data or formal LOCO result exists. Phase 8.2 owns those operational artifacts so that server/formal evidence is not appended to the frozen local-repair phase.

## Fixed inputs

- Experiment ID: `phase8.1-main-identification-v1`.
- Transition version: `easyuuv-koopman-transition-v2.1`.
- Role SHA-256: `083d5eae3729e9939287345ab258dbfe4b4c8ca71ab769c2fd8616431a649417`.
- Analysis SHA-256: `7a790b43d0f1581b8995ccdcbd9b6d259cb09bc8fe2268201400243d05f1e18c`.
- Canonical approval: `protocols/phase8_1/d23_approval.json`.
- Eight configurations and all candidate, metric, bootstrap, promotion and claim semantics remain exactly those frozen by Phase 8.1 D-23.

## Operational topology

| Level | Root | Ownership |
|---|---|---|
| Local transfer | `.pytest-tmp/phase8-2-transfer/` | disposable bundle and immutable sidecars |
| Server checkout | `/root/EASYkoopman-phase8-2-v1` | clean bundle checkout only |
| Server fresh result | `/root/EASYkoopman-phase8-2-results-v1` | collection/status; kept outside checkout so provenance stays clean |
| Local canonical | `source/results/koopman_phase8_2/` | dataset, evaluation, selection and closeout after staged validation |

The server has no required GitHub access. Transfer uses a verified Git bundle. No SSH occurs until Plan 08.2-01 completes on a clean committed HEAD and the user starts the unchanged server.

## Evidence sequence

```text
canonical D-23 validation
  -> local targeted/relevant tests
  -> clean committed HEAD
  -> offline bundle create/verify/clone
  -> explicit server-start checkpoint
  -> fresh v2.1 server collection
  -> staged pullback + exact inventory/split validation
  -> formal eight-fold LOCO
  -> outer SELECTION / NO_SELECTION
  -> independent closeout
```

The held-out test remains unreadable until each fold has sealed its source ledger and frozen primary/expert state. Server collection only creates data; it does not run candidate selection or open test data for model decisions.

## Claim boundary

Phase 8.2 may only evaluate transfer to the held-out member of the fixed exact-eight catalog on fresh episodes from the repaired simulator. It cannot support claims about researcher-unseen configurations, arbitrary AUVs, environment transfer, closed-loop control, hardware/Sim2Real or Agent capability.
