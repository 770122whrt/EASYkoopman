---
phase: 08.2-phase-8-1-fresh-server-evaluation-and-closeout
verified: 2026-09-01T15:55:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
terminal_verdict: PASS_VALID_FROZEN_NO_SELECTION
selection_status: no_selection
human_verification: []
---

# Phase 8.2: Fresh Server Evaluation and Closeout Verification

**Phase Goal:** 从 clean committed HEAD 构建可离线验证的 bundle，在现有 Isaac 服务器上重新采集完整 v2.1 exact-eight 数据，经 staged pullback/inventory/split 验证后执行冻结的正式 8-fold LOCO、outer `SELECTION/NO_SELECTION` 与独立 closeout。

**Verified:** 2026-09-01T15:55:00Z
**Status:** `passed`
**Re-verification:** No — initial independent verification
**Verdict:** `PASS_VALID_FROZEN_NO_SELECTION`

本报告从 KIDO-01..05 和 ROADMAP success criteria 反向检查 canonical 代码与证据。
没有使用 SUMMARY 作为实现证据；没有 SSH、重采集、改动
dataset/evaluation/selection/closeout bytes；没有进入 Phase 9。

## Terminal Interpretation

Phase 8.2 的研究目标已完成，终局为合法负结果：八折证据链有效，冻结门判定两个
eligible families 均不可晋升，terminal artifact 是无模型路径的 `NO_SELECTION`。

- `pooled_koopman_v21` 与 `simple_linear` 在全部 24 条 held-out test episodes 上
  六项 full-rollout primary metrics 逐位相同（差值 < 1e-12）；对基线的
  `baseline_gates_pass=false`。v2.1 的 19D 状态（含 actuator memory）没有打破
  估计等价类。
- 两个线性族在六项官方指标上全面差于 persistence 零阶保持基线
  （aggregate 0.5061/0.0966/0.8376/1.2977/1.4236/2.1764 vs
  0.4728/0.0846/0.7613/1.0731/1.1453/1.7352，n=24）。
- `conditional_koopman_v21` 在全部 8 折出现 reason-coded
  `source_candidate_unavailable`（条件族候选在源阶段即不可用），family
  fail closed。
- `selection_result.json` 的 `selected_family=null`、`selected_model_path=null`，
  reason codes 为 `baseline_improvement_failed` 和 `role_failed`。
- 这证明的是"冻结候选没有通过跨构型预测晋升门"，并不证明 Koopman-MPC、
  closed-loop、环境迁移、Agentic、Sim2Real 或硬件有效性。

## Goal Achievement

### KIDO Requirements

| Requirement | Status | Goal-backward evidence |
|---|---|---|
| `KIDO-01` versioned local operational readiness | **PASS** | Preflight/bundle/bootstrap/collection/pullback/formal/closeout 脚本与 runbook 全部 checked-in（`a184008`+`1151b21`）；preflight 验证 canonical D-23、32 targeted/200 relevant tests、protected history、clean HEAD、canonical roots 缺席；bundle 通过 `git bundle verify` 与离线 clone。 |
| `KIDO-02` fresh v2.1 server collection | **PASS** | `v1` 失败尝试保留于 `/root/EASYkoopman-phase8-2-v1`（bootstrap readonly 变量重赋值，采集前停止）；`v2` fresh roots 完成 8×12=96 episodes：96 JSONL/96 manifests/96 logs、零 `.part`、49,152 transitions；native/tee/semantic gates 全过后才建 inventory。 |
| `KIDO-03` pullback, inventory and split validation | **PASS** | 随机 staging 验证 commit/协议/approval/相对路径 SHA-256 exact set/96 episode schema/inventory/split 后原子提升 `dataset/`+`collection_status/`；dataset 提交 `fa2fda0`+`460c80a`；独立复核 `validate_phase82_dataset_index.py` 通过（96 episodes）。 |
| `KIDO-04` formal 8-fold LOCO and outer decision | **PASS** | 8 个独立进程各执行 source7 fit/validation → ledger seal → primary freeze → held-out diagnostic → expert freeze → test authorization → guarded held-out test，每折 staging 原子发布；assemble 前重验 D-23/dataset/clean-tree；8 折全部 `formal_fold_resume` 复用（无重算），canonical `evaluation/` 原子生成，outer selector 输出 pathless `NO_SELECTION`。 |
| `KIDO-05` independent closeout | **PASS** | `close_phase82.py` 复核 approval/dataset/evaluation/selection envelopes、8-fold 完整性、终局路径语义与 claim boundary，输出 `closeout/closeout.json`（`status=VERIFIED`）。 |

**Score:** 5/5 KIDO requirements verified.

### ROADMAP Success Criteria

| # | Criterion | Status | Evidence |
|---:|---|---|---|
| 1 | pre-server readiness（approval/tests/scripts/bundle） | **VERIFIED** | Plan 08.2-01 closeout 记录；本阶段未重复 SSH。 |
| 2 | 8×12 episodes；任一 gate 失败不产生 canonical success | **VERIFIED** | collection_status 8×3 状态文件 + `collection_complete.status`；pullback 前全量验证。 |
| 3 | 全验证后从随机 staging 原子提升 | **VERIFIED** | pullback 脚本序列 + promoted dataset 独立复验通过（96 episodes valid）。 |
| 4 | source-only selection、pre-test freeze、guarded test、冻结终局 | **VERIFIED** | 每折 `pre_test_freeze.json` 在 test 访问前封存（token 授权）；outer selector 输出 `NO_SELECTION`（pathless）。 |
| 5 | closeout 限定 exact-eight fresh-episode prediction-transfer 结论 | **VERIFIED** | `closeout.json` 的 `allowed_claim=fixed_exact_eight_catalog_new_episode_loco_prediction_evaluation`，`model_handoff=false`。 |

## Frozen Provenance

| Artifact | Binding |
|---|---|
| Role protocol | `083d5eae3729e9939287345ab258dbfe4b4c8ca71ab769c2fd8616431a649417` |
| Analysis policy | `7a790b43d0f1581b8995ccdcbd9b6d259cb09bc8fe2268201400243d05f1e18c` |
| Dataset envelope | commits `fa2fda0` + `460c80a`（inventory SHA-256 记录于 closeout.json） |
| Evaluation envelope | commit `ce622c0`（envelope SHA-256 记录于 closeout.json） |
| Selection result | commit `ce622c0`（result SHA-256 由 selection_envelope 绑定） |
| Selection envelope | commit `ce622c0`（envelope SHA-256 记录于 closeout.json） |
| Closeout artifact | commit `ab3c867` |

Source commits: dataset `4f3b4cb`，evaluator/selector `1151b21`。post-collection
drift 检查只允许已声明的 8 个 repair 路径（全部已提交于 `1151b21`），closeout
侧同一 allowlist 由 `384caf1` 对齐。

## Required Artifacts

| Artifact | Exists | Substantive | Wired | Status |
|---|---:|---:|---:|---|
| `source/results/koopman_phase8_2/dataset/` (+ inventory/split/envelope) | yes | 96 episodes、exact-eight split | evaluator 前置校验加载 | **VERIFIED** |
| `source/results/koopman_phase8_2/collection_status/` | yes | 8×3 gates + 完成状态 + 环境 provenance | 采集链操作 provenance | **VERIFIED** |
| `source/results/koopman_phase8_2/evaluation/` | yes | 8 fold envelope + 汇总 | selector 唯一科学输入 | **VERIFIED** |
| `source/results/koopman_phase8_2/selection/` | yes | selection_result + envelope | 终局 artifact | **VERIFIED** |
| `source/results/koopman_phase8_2/closeout/` | yes | closeout.json（VERIFIED） | 独立复核输出 | **VERIFIED** |
| `workflows/run_koopman_v21_loco.py` / `select_koopman_v21.py` / `close_phase82.py` | yes | 冻结协议执行层 | checked-in 脚本链调用 | **VERIFIED** |

## Behavioral Checks Re-run

| Check | Result | Status |
|---|---|---|
| `validate_phase81_d23_approval.py` | `phase81_d23_approval_valid` | **PASS** |
| `validate_phase82_dataset_index.py` | 96 episodes，`phase82_dataset_index_valid` | **PASS** |
| `scripts/phase8_2_formal_local.ps1` (assembly from completed folds) | `phase8_2_formal_chain=complete`, 8× `formal_fold_resume` | **PASS** |
| `scripts/phase8_2_closeout_local.ps1` | `phase8_2_closeout=pass` | **PASS** |
| `closeout/closeout.json` | `status=VERIFIED`, `terminal_decision=NO_SELECTION`, `model_handoff=false` | **PASS** |

## Adversarial/Anti-Pattern Review

| Finding | Classification | Disposition |
|---|---|---|
| 8 折并发编排偏离 checked-in 串行脚本（仓库外 v4 调度器） | Info, not a stub | 每折仍是独立进程、独立 staging、原子发布、CLI 绑定与 checked-in 链完全一致；assemble/selection 仍由 checked-in `phase8_2_formal_local.ps1` 在全部预检后执行（8× resume，无重算）。plan 08.2-03 的要求是"每折独立 Python 进程"，并发满足该要求。 |
| v4 调度器把 8 个成功折误报为 failed 并 fail-closed | Info | 遥测 `exit_code=0`×8 + fold envelope 齐全证明折全部成功；误报只导致调度器放弃自动组装，由 checked-in 脚本接手完成。折级证据不受影响。 |
| closeout 首跑 fail-closed：漂移门禁缺 post-collection allowlist | Warning, fixed | `384caf1` 将 formal 脚本已提交的同一 allowlist 镜像进 closeout 脚本（含其自身路径）；无新豁免；修复后 closeout 通过。 |
| 多次宿主休眠/重启/会话关闭中断历史尝试 | Info | 中断只留下空 staging（PID 命名），无 partial fold 曾被发布；最终 8 折全部来自无中断的 v4 运行（2026-08-31 23:14 至 2026-09-01 21:49）。 |
| conditional family 全 8 折失败 | Fail-closed by design | 失败折保持显式 reason-coded 证据（`source_candidate_unavailable`），未被聚合隐藏，family 完整 ineligible。 |
| TODO/FIXME/placeholder scan | None blocking | 正式链入口（formal/closeout 脚本、v21 执行层）无占位实现。 |

## Human Verification

None. Deterministic offline evidence/selection pipeline with machine-readable
artifacts; no visual or external-service behavior. Isaac collection provenance
is carried by the already pulled and revalidated server envelope.

## Gaps and Claim Boundary

No unresolved Phase 8.2 blocker was found. KIDO-01..05 are satisfied by a valid
negative outcome, not by claiming a transferable model exists.

There is no Phase 9 handoff model. Phase 9 must not start from this artifact; any
later identification attempt must use a new versioned experiment and must not
rewrite or retune this frozen result. Each held-out configuration has three test
episodes: the negative outcome means "no ≥1% improvement detected under the
frozen gates", not "lifting is useless".

Allowed claim: on fresh episodes from the repaired simulator, whether a model
trained and selected using the other seven members transfers to the held-out
eighth member of the fixed exact-eight catalog. All broader claims remain
prohibited.

No protocol deviation.

---

_Verifier: independent closeout (Phase 8.2 Gate 5)_
_Terminal verdict: `PASS_VALID_FROZEN_NO_SELECTION`_
