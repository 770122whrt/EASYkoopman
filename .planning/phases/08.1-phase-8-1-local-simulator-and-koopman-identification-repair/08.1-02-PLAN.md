---
phase: 08.1-phase-8-1-local-simulator-and-koopman-identification-repair
plan: 02
type: tdd
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - koopman/so3_v21.py
  - koopman/platform_features_v21.py
  - koopman/model_v21.py
  - koopman/metrics_v21.py
  - tests/test_koopman_so3_v21.py
  - tests/test_koopman_platform_features_v21.py
  - tests/test_koopman_model_v21.py
  - tests/test_koopman_metrics_v21.py
autonomous: true
requirements: [KIDR-04, KIDR-05, KIDR-06]
must_haves:
  truths:
    - "Orientation targets and rollouts use deterministic body-right SO(3) Log/Exp, not quaternion-component recursion or projection repair."
    - "Observable and conditional designs have exact frozen names/order/formulas and widths 22/56/66/100."
    - "Conditional PCA and solve admission use seven-source-only statistics; held-out descriptors and future memory truth are structurally unavailable."
  artifacts:
    - path: "koopman/so3_v21.py"
      provides: "deterministic quaternion/R6/Log/Exp/body-right operations"
    - path: "koopman/platform_features_v21.py"
      provides: "physical-core normalization, deterministic PCA2 and held-out diagnostics"
    - path: "koopman/model_v21.py"
      provides: "SO(3) identity/kinematic pooled and structured-conditional estimators"
    - path: "koopman/metrics_v21.py"
      provides: "causal rollout and official sign-invariant metrics"
  key_links:
    - from: "koopman/model_v21.py"
      to: "koopman/so3_v21.py"
      via: "10D increment targets and body-right reconstruction"
      pattern: "log|exp|body_right"
    - from: "koopman/metrics_v21.py"
      to: "koopman/actuator_memory_v21.py"
      via: "window-start memory followed by causal advance"
      pattern: "ActuatorMemoryProxyV21"
    - from: "koopman/model_v21.py"
      to: "koopman/platform_features_v21.py"
      via: "frozen source-only PCA transform and conditional z1/z2 interactions"
      pattern: "platform.*v21"
---

<objective>
Build the deterministic SO(3), observable, structured-conditional and causal-rollout backend on the Plan 01 data contract.

Purpose: Remove quaternion projection as a normal model mechanism and prevent the old rank-deficient full platform interaction from reappearing.
Output: Tested additive model stack with exact features, numerical admission diagnostics and honest estimator-equivalence metadata.
</objective>

<execution_context>
@C:/Users/rt do believe/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/rt do believe/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08.1-phase-8-1-local-simulator-and-koopman-identification-repair/08.1-SPEC.md
@.planning/phases/08.1-phase-8-1-local-simulator-and-koopman-identification-repair/08.1-CONTEXT.md
@koopman/actuator_memory_v21.py
@koopman/dataset_v21.py
@koopman/platform_features_v2.py
@koopman/model_v2.py
@koopman/metrics_v2.py

<interfaces>
External primary row: `[state_11, actuator_memory_4, virtual_control_4]` (19D).
Target: `[delta_depth, delta_linear_velocity_body_3, delta_angular_velocity_body_3, delta_theta_body_3]` (10D).
Observable names and widths: `so3_identity_v1=22`, `so3_kinematic_v1=56`; conditional widths: 66 and 100.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN deterministic SO(3) primitives and increment targets</name>
  <files>tests/test_koopman_so3_v21.py, koopman/so3_v21.py</files>
  <behavior>
    - `[w,x,y,z]` Hamilton body-to-world quaternions produce R6 first two columns in column-major order.
    - q and -q canonicalize identically; 0/90/180 degree Log/Exp and noncommutative body-right cases are deterministic.
    - Near-zero series are finite and continuous; exact-pi axis sign uses largest-absolute-component/lowest-index tie-break.
    - Invalid shape/nonfinite/norm<1e-10/norm error>1e-6 fails `quaternion_invalid`; composition drift>1e-10 fails `quaternion_unit_norm_drift`.
  </behavior>
  <action>Write the complete edge-case suite first and observe import/behavior RED. Implement D81-10..12 exactly as frozen in SPEC: input gate then binary64 normalization, canonical principal Log, approved near-zero series, deterministic pi tie, Exp series and Hamilton body-right composition. Budget-only renormalization after composition is allowed; do not add a projection fallback. Expose sign-invariant SO(3) geodesic radians for metrics and keep all normal-path failures reason-coded.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_so3_v21.py --basetemp .pytest-tmp\phase81-02-so3</automated>
  </verify>
  <done>SO(3) primitives and 10D targets are unique at q/-q and pi, stable near zero, body-right correct and fail closed outside numerical budgets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN frozen observables, physical PCA2 and solve admission</name>
  <files>tests/test_koopman_platform_features_v21.py, tests/test_koopman_model_v21.py, koopman/platform_features_v21.py, koopman/model_v21.py</files>
  <behavior>
    - Physical core has the exact 19 names/order/formulas; identity labels/configuration hashes never enter it.
    - Equal-configuration population normalization, exact-zero variance removal, deterministic tied eigenspace basis/sign and PCA r=2/no-whitening use exactly seven source rows.
    - Observable features are exactly 22/56 and the 34 lifts follow the frozen order; conditional designs are exactly 66/100.
    - Actual normalized solve design must be full column rank under the frozen tolerance and regularized_condition<=1e8; ridge includes bias and effective rank is diagnostic only.
  </behavior>
  <action>RED first with feature-name/order/formula fixtures, row-order/tied-eigenvalue mutations, held-out-statistic poison data, width assertions and rank/condition rejection cases. Implement `platform_physical_core_v1`, source-only population normalization and deterministic projector-based PCA per D81-15..19. Per D81-13, implement only the approved 22D identity and the 21D nonbias plus 34D handcrafted Koopman-style lift exactly as SPEC, then assemble pooled/conditional ridge designs without duplicate bias/platform columns. Preserve bias=1 under standard normalization, include bias in ridge, compute diagnostics from the actual normalized solve design, and use stable rejection reasons. Record `so3_identity_v1` and SO(3) simple-linear as one estimator equivalence class; do not claim invariant-subspace, hydrodynamic, identifiability or OOD proof.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_platform_features_v21.py tests/test_koopman_model_v21.py --basetemp .pytest-tmp\phase81-02-model</automated>
  </verify>
  <done>Feature/PCA/conditional design is source-only and deterministic, exact widths pass, and rank/condition admission rejects deficient candidates before selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED→GREEN causal SO(3) multi-step rollout and official metrics</name>
  <files>tests/test_koopman_metrics_v21.py, koopman/metrics_v21.py</files>
  <behavior>
    - Rollout reads true actuator memory only at the window start, then advances the production proxy with recorded future controls.
    - Mutating all future memory-truth rows leaves predictions and metrics byte-equivalent.
    - Quaternion norm stays within budget over multiple steps; illegal quaternion/nonfinite/divergence failures remain reason-coded.
    - 5/20/60/full results retain sign-invariant SO(3) mean/RMSE/max radians and configuration-balanced aggregation seams.
    - The only accepted rollout divergence policy inherits Phase 8 depth/linear/angular absolute maxima `100/100/100`; its payload/hash is protocol-bound.
  </behavior>
  <action>RED with a spy dataset that raises on any future-memory access and a mutation pair differing only in those forbidden rows. GREEN by implementing one shared v2.1 rollout seam used by later source and held-out evaluation: start from observed state/memory, predict the 10D increment, compose orientation with `so3_v21`, update the production `ActuatorMemoryProxyV21`, and consume only the control sequence. Do not copy recurrence or add quaternion projection. Preserve the six inherited primary metrics and horizon/aggregation names per D81-06, D81-14 and D81-28; reject caller-substituted divergence thresholds rather than silently changing the frozen invalid/divergence sentinel.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_metrics_v21.py tests/test_koopman_metrics_v2.py --basetemp .pytest-tmp\phase81-02-rollout</automated>
  </verify>
  <done>Source and future held-out evaluators have one causal SO(3) rollout seam, no future-memory truth access and unchanged official metric semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Dataset quaternion/control → rollout | Invalid orientation or future memory can corrupt recursive predictions. |
| Seven-source descriptors → PCA/model | Held-out identity/statistics can leak into candidate design. |
| Normalized design → ridge solver | Rank-deficient designs can be numerically solvable but scientifically inadmissible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-81-04 | Tampering | SO(3) target/rollout | mitigate | Exact canonicalization, norm gates and reason-coded fail-closed composition. |
| T-81-05 | Information disclosure | source-only PCA | mitigate | API accepts only frozen seven-source descriptors; held-out poison mutations must not affect transform bytes. |
| T-81-06 | Elevation of privilege | conditional admission | mitigate | Reject pre-ridge rank deficiency and excessive regularized condition before candidate ranking. |
</threat_model>

<verification>
Run each targeted command after its RED→GREEN cycle, then run all four v2.1 model test files together. Do not inspect or load old held-out test artifacts while implementing features, PCA, gates or thresholds.
</verification>

<success_criteria>
- KIDR-04 deterministic SO(3) recursion replaces arbitrary quaternion regression/projection.
- KIDR-05 exact frozen observables and structured conditional pass source-only numerical admission.
- KIDR-06 rollout causality and estimator-equivalence evidence are machine-verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-phase-8-1-local-simulator-and-koopman-identification-repair/08.1-02-SUMMARY.md` with RED/GREEN evidence, exact widths, PCA/rank diagnostics, rollout causality and deviations.
</output>
