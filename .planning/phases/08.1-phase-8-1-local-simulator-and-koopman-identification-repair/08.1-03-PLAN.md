---
phase: 08.1-phase-8-1-local-simulator-and-koopman-identification-repair
plan: 03
type: tdd
wave: 3
depends_on: ["08.1-02"]
files_modified:
  - koopman/loco_v21.py
  - koopman/evaluation_v21.py
  - koopman/selection_v21.py
  - tests/test_koopman_loco_v21.py
  - tests/test_koopman_evaluation_v21.py
  - tests/test_koopman_selection_v21.py
autonomous: true
requirements: [KIDR-06, KIDR-07, KIDR-08]
must_haves:
  truths:
    - "Every source candidate, including rejected candidates, is sealed before held-out descriptor or test access."
    - "Held-out diagnostic and independently tuned expert are selection-ineligible and cannot mutate the primary freeze."
    - "A positive family decision can complete an all-eight fit/validation-only final refit and atomic publication with zero test reads."
  artifacts:
    - path: "koopman/loco_v21.py"
      provides: "source-only grid, complete candidate ledger, freeze/test authorization state machine"
    - path: "koopman/evaluation_v21.py"
      provides: "shared rollout evaluation, isolated held-out diagnostic and expert namespace"
    - path: "koopman/selection_v21.py"
      provides: "family-only final refit and atomic selection/no-selection publication"
  key_links:
    - from: "koopman/loco_v21.py"
      to: "source_candidate_ledger.json"
      via: "seal complete source-only ranking before freeze"
      pattern: "source_candidate_ledger"
    - from: "koopman/evaluation_v21.py"
      to: "koopman/metrics_v21.py"
      via: "one rollout seam for source/expert/held-out metrics"
      pattern: "metrics_v21"
    - from: "koopman/selection_v21.py"
      to: "koopman/loco_v21.py"
      via: "same inner selector over all-eight fit/validation and selected_family only"
      pattern: "selected_family"
---

<objective>
Implement the complete source-only selection, diagnostic/expert isolation and test-free positive publication state machine on synthetic local data.

Purpose: Make pre-test freeze and publication correctness executable without opening any formal Phase 8.1 test data.
Output: Complete ledgers, isolated diagnostic/expert artifacts and a family-only final-refit publisher.
</objective>

<execution_context>
@C:/Users/rt do believe/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/rt do believe/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08.1-phase-8-1-local-simulator-and-koopman-identification-repair/08.1-SPEC.md
@.planning/phases/08.1-phase-8-1-local-simulator-and-koopman-identification-repair/08.1-CONTEXT.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-SPEC.md
@.planning/phases/08-multi-configuration-koopman-identification-and-ood-gate/08-05-PLAN.md
@protocols/phase8/analysis_policy.json
@koopman/loco_v2.py
@koopman/evaluation_v2.py
@koopman/selection_v2.py
@koopman/model_v21.py
@koopman/metrics_v21.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN complete source candidate ledger and primary freeze</name>
  <files>tests/test_koopman_loco_v21.py, koopman/loco_v21.py</files>
  <behavior>
    - Ledger contains every successful/rejected candidate, seven source configurations x six primary metrics, both baselines, ratios/source_score, numerical diagnostics/reason and deterministic source-only ranking.
    - The selected candidate and model are immutable after ledger seal; test access before the later diagnostic/expert sequence completes fails closed, including the state immediately after primary freeze.
    - Selector signature has no held-out diagnostic path/value and identity/simple-linear equivalence is explicit.
    - source-per-configuration mode is `report_all_no_ranking`.
  </behavior>
  <action>RED first with success, rank rejection, denominator-zero, missing-candidate, held-out-stat injection and post-seal mutation fixtures. GREEN per D81-14 and D81-20..23: evaluate all frozen candidates on seven-source fit/validation only; compute six independent robust-relative ratios against the better baseline using inherited equal-configuration macro and deterministic zero-denominator handling, then set the unweighted `source_score=max(the six ratios)`; do not introduce metric weights. Record rank/effective-rank/width/condition/reason; seal `source_candidate_ledger.json`; then freeze candidate/model/normalizer/PCA and leave the state explicitly authorization-ineligible. Add a test proving that primary freeze alone still cannot authorize or open test. This task must not issue a test token; Task 2 exclusively owns held-out diagnostic, expert selection/freeze, authorization and open. The selector API must be structurally unable to accept a held-out diagnostic. Keep old Phase 8 selection bytes and artifacts untouched.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_loco_v21.py --basetemp .pytest-tmp\phase81-03-ledger</automated>
  </verify>
  <done>A complete source-only ledger and immutable primary freeze exist before any held-out descriptor/test access, remain authorization-ineligible, and have stable rejection reasons with no hidden ranking inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN held-out diagnostic isolation and independently selected expert</name>
  <files>tests/test_koopman_evaluation_v21.py, koopman/evaluation_v21.py</files>
  <behavior>
    - heldout_descriptor_diagnostic is generated only after primary freeze, has `selection_eligible=false`, and never changes source ledger/ranking/decision hashes.
    - Expert uses C/fit for fitting and C/validation for its own hyperparameter selection, with no conditional features and an independent ledger/model/normalizer/freeze.
    - Expert mutation/failure cannot promote or block primary test authorization.
    - Source and held-out roles call the same causal rollout seam.
  </behavior>
  <action>Create RED state-machine, mutation and file-access-spy tests. Implement the exact D81-20..23 order: source grid → sealed source ledger → primary freeze → held-out descriptor diagnostic → expert fit/validation selection/freeze → primary test authorization → test open. The diagnostic reuses the frozen source transform for range/convex-hull/PCA-residual reporting and cannot write back. Expert candidates are nonconditional, selected independently using only held-out fit/validation, reside under their own namespace and are permanently nonpromoting. Failure produces a diagnostic reason but does not block a valid primary test token. Use `metrics_v21` for every rollout.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_evaluation_v21.py tests/test_koopman_loco_v21.py --basetemp .pytest-tmp\phase81-03-eval</automated>
  </verify>
  <done>Diagnostic/expert artifacts are temporally and type-isolated, expert selection is genuine, and primary test authorization remains bound only to frozen primary state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED→GREEN outer family gate, selected-family-only final refit and atomic publication</name>
  <files>tests/test_koopman_selection_v21.py, koopman/selection_v21.py</files>
  <behavior>
    - An outer selector consumes only complete eight-fold eligible pooled/conditional aggregate evidence, applies the unchanged six-metric/bootstrap/threshold promotion policy, and outputs only pooled, conditional or NO_SELECTION.
    - Public refit API accepts only selected_family; it cannot accept fold candidate IDs, test metrics, threshold overrides, fold voting or expert results.
    - All-eight fit/validation reruns the same source selector within the selected family, then final fit uses fit+validation and zero test reads.
    - Save/load roundtrip precedes atomic publication; any failure publishes a pathless reason-coded NO_SELECTION and no staged model leaks.
    - Synthetic positive path may select a candidate different from all fold winners without consulting outer-test values.
  </behavior>
  <action>RED first with synthetic outer positive/negative cases, incomplete-fold and ineligible expert/per-configuration evidence, frozen-threshold/bootstrap parity, signature-introspection, poisoned-test-reader, candidate-difference, roundtrip-failure and interrupted-publication tests. GREEN per D81-24 in two type-separated steps. First implement an outer-family selector that requires all eight completed folds and only eligible pooled/conditional aggregate evidence, reuses the frozen Phase 8 six metrics, configuration-stratified bootstrap, minimum-improvement 0.01, noninferiority 0.1, conditional margin 0.05 and invalid/divergence/nonfinite gates, and emits only `pooled`, `conditional` or `NO_SELECTION`; expert and source-per-configuration artifacts are structurally ineligible. Then pass only a positive `selected_family` to the public final-refit API: reopen only all-eight fit and validation roles; invoke the same registered inner selector restricted to that family; fit on those non-test roles; verify opened-file ledger has zero test reads and serialized model/normalizer/PCA roundtrip; publish from a confined fresh staging directory with atomic replace. On any failure discard model bytes and emit pathless `NO_SELECTION`. Do not pass outer metrics, copy/vote fold settings or permit threshold/test/expert parameters into final refit.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_selection_v21.py --basetemp .pytest-tmp\phase81-03-select</automated>
  </verify>
  <done>Synthetic outer evidence yields the frozen-policy family/NO_SELECTION decision, and positive/negative publication paths are atomic, test-free and family-only without changing promotion thresholds or bootstrap semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source fit/validation → selector | Only seven configurations may influence candidate selection and normalization. |
| Frozen primary → held-out diagnostic/expert/test | Post-freeze artifacts must not gain selection authority. |
| Outer family result → final refit | Test-derived detail must be reduced to family identity only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-81-07 | Elevation of privilege | held-out diagnostic/expert | mitigate | Selection-ineligible types, selector signature exclusion and mutation tests. |
| T-81-08 | Tampering | sealed ledger/frozen model | mitigate | Immutable state and assertions prevent post-seal/post-test mutation. |
| T-81-09 | Repudiation | final refit file access | mitigate | Opened-file audit records all fit/validation reads and proves zero test reads. |
</threat_model>

<verification>
Run targeted suites in task order, then run the three v2.1 evaluation/selection test files together. Use synthetic local fixtures only; do not read canonical old held-out test evidence or execute formal eight-fold LOCO.
</verification>

<success_criteria>
- KIDR-07 complete ledger, diagnostic isolation and independently tuned nonpromoting expert pass.
- KIDR-08 selected-family-only final refit completes or yields pathless NO_SELECTION atomically with zero test reads.
- No test result changes the frozen candidate, threshold, grid, PCA dimension or experiment design.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-phase-8-1-local-simulator-and-koopman-identification-repair/08.1-03-SUMMARY.md` with ledger completeness, state transitions, expert independence, zero-test-read and atomic publication test evidence.
</output>
