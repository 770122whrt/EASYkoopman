---
phase: 08.1-phase-8-1-local-simulator-and-koopman-identification-repair
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - easyuuv_nc/env/thruster_dynamics.py
  - easyuuv_nc/env/easyuuv_env.py
  - koopman/actuator_memory_v21.py
  - koopman/schema_v21.py
  - koopman/dataset_v21.py
  - workflows/koopman_bridge_v21.py
  - tests/test_thruster_dynamics.py
  - tests/test_koopman_actuator_memory_v21.py
  - tests/test_koopman_schema_v21.py
  - tests/test_koopman_dataset_v21.py
  - tests/test_koopman_bridge_v21.py
autonomous: true
requirements: [KIDR-01, KIDR-02, KIDR-03]
must_haves:
  truths:
    - "Reset 后第一个 physics substep 获得完整 physics_dt，D 个 substeps 与解析一阶响应一致。"
    - "v2.1 transition 唯一记录施加当前控制前的 actuator_memory_4，并暴露精确 19D primary view。"
    - "v2.1 timing provenance 绑定 physics_dt_s、整数 decimation 和 control_dt_s，旧 v2 bytes/API 不变。"
  artifacts:
    - path: "koopman/actuator_memory_v21.py"
      provides: "唯一 binary64 causal actuator-memory recurrence"
      exports: ["ActuatorMemoryProxyV21"]
    - path: "koopman/schema_v21.py"
      provides: "strict easyuuv-koopman-transition-v2.1 schema/logger/validator"
    - path: "koopman/dataset_v21.py"
      provides: "19D primary dataset view and diagnostic separation"
    - path: "workflows/koopman_bridge_v21.py"
      provides: "state-memory-control-next-state transition timing seam"
  key_links:
    - from: "easyuuv_nc/env/easyuuv_env.py"
      to: "easyuuv_nc/env/thruster_dynamics.py"
      via: "physics-substep end_time update followed by clock commit"
      pattern: "end_time.*thruster_dynamics\.update"
    - from: "workflows/koopman_bridge_v21.py"
      to: "koopman/actuator_memory_v21.py"
      via: "current before row emission, advance after masked control"
      pattern: "\.current\(|\.advance\("
    - from: "koopman/dataset_v21.py"
      to: "koopman/schema_v21.py"
      via: "strict version validation before 19D view construction"
      pattern: "validate_.*v21"
---

<objective>
Repair the shared actuator clock and establish the additive causal data contract that every later Phase 8.1 model consumes.

Purpose: Close the first-substep timing defect and make actuator memory causal, topology-neutral and version-isolated before any model work.
Output: Correct substep dynamics plus strict actuator-memory, schema, dataset and Bridge modules with targeted RED→GREEN evidence.
</objective>

<execution_context>
@C:/Users/rt do believe/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/rt do believe/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-phase-8-1-local-simulator-and-koopman-identification-repair/08.1-SPEC.md
@.planning/phases/08.1-phase-8-1-local-simulator-and-koopman-identification-repair/08.1-CONTEXT.md
@.planning/phases/07-cross-configuration-koopman-data-and-control-contract/07-SPEC.md
@easyuuv_nc/env/thruster_dynamics.py
@easyuuv_nc/env/easyuuv_env.py
@koopman/schema_v2.py
@koopman/dataset_v2.py
@workflows/koopman_bridge_v2.py

<interfaces>
Create exactly:
```python
class ActuatorMemoryProxyV21:
    def __init__(self, tau_s, control_dt_s, control_mask_4): ...
    def reset(self) -> None: ...
    def current(self) -> numpy.ndarray: ...
    def advance(self, masked_virtual_control_t) -> numpy.ndarray: ...
```
The transition version is `easyuuv-koopman-transition-v2.1`; primary input ordering is exactly `[state_11, actuator_memory_4, virtual_control_4]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN the physics-substep end-time actuator clock</name>
  <files>tests/test_thruster_dynamics.py, easyuuv_nc/env/thruster_dynamics.py, easyuuv_nc/env/easyuuv_env.py</files>
  <behavior>
    - Reset establishes state=0, previous_end_time_s=0 and selected-environment clock=0.
    - The first command update at end_time=physics_dt produces `1-exp(-physics_dt/tau)`, while an explicit dt=0 update preserves state.
    - D constant-command substeps match the independent closed form within `8*eps(float32)`; larger tau rises more slowly.
    - Backward time, invalid tau/shape/device/dtype/nonfinite values fail closed; selected resets do not affect other environments.
  </behavior>
  <action>Per D81-04, first change/add the targeted tests so they fail specifically because the current first call receives dt=0 and the environment updates before advancing its clock. Record that RED result. Then set reset/constructor `prevTime` to exact zero, remove the uninitialized-time-origin path, compute `dt=t-prevTime`, and keep monotonic/finite/shape/device/dtype gates. In the environment compute `end_time_s=_thruster_dynamics_time_s+physics_dt_s`, call `update(command,end_time_s)`, and commit the clock only after successful update. Use an independently coded analytic expectation in tests; do not restore any forced-alpha path. Preserve unrelated environment edits and all old Phase 8 evidence.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_thruster_dynamics.py --basetemp .pytest-tmp\phase81-01-clock</automated>
  </verify>
  <done>First-substep, dt=0, D-substep, reset isolation, different-tau and invalid-input contracts pass, with no `alpha=0` or first-call-zero special path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN the unique actuator-memory module and strict schema/dataset contract</name>
  <files>tests/test_koopman_actuator_memory_v21.py, tests/test_koopman_schema_v21.py, tests/test_koopman_dataset_v21.py, koopman/actuator_memory_v21.py, koopman/schema_v21.py, koopman/dataset_v21.py</files>
  <behavior>
    - Binary64 reset is exact zero; recurrence uses masked input and matches independent analytics at atol=rtol=1e-12.
    - A nonzero disabled channel fails closed and `uuv4*` yaw control/memory remains exact zero.
    - Timing provenance accepts only positive physics_dt_s/control_dt_s, positive integer decimation and exact binary64 product relation.
    - JSON roundtrip preserves binary64 values; v2 and v2.1 validators reject each other's bytes.
    - Model-facing arrays are X=19D and Y=next-state contract only; PWM/wrench/oracle remain named diagnostics and cannot become inputs.
  </behavior>
  <action>Write failing tests before production files. Implement `ActuatorMemoryProxyV21` as the only recurrence owner per D81-05..09, returning defensive binary64 values and applying the frozen mask before update. Implement additive strict v2.1 transition/episode/manifest/logger validation around exact timing `(state[t],memory[t],control[t],state[t+1])`, exact version, exact field sets, finite shapes, exact-zero first row and episode-local recurrence; validator must call the module rather than copy the formula. Implement `KoopmanDatasetV21` with immutable 19D primary view and explicit diagnostic accessors only. Reuse v2 pure helpers only where they cannot make v2 accept v2.1. Never describe the proxy as thrust, PWM, wrench, actuator truth or a proof of a strictly Markov plant.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_actuator_memory_v21.py tests/test_koopman_schema_v21.py tests/test_koopman_dataset_v21.py tests/test_koopman_schema_v2.py tests/test_koopman_dataset_v2.py --basetemp .pytest-tmp\phase81-01-data</automated>
  </verify>
  <done>The recurrence, strict v2.1 serialization/timing, 19D view and diagnostic exclusion pass while canonical v2 validation and dataset tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED→GREEN Bridge timing, reset and simulator/proxy equivalence</name>
  <files>tests/test_koopman_bridge_v21.py, workflows/koopman_bridge_v21.py</files>
  <behavior>
    - Bridge emits `current()` before applying the row control, then advances once after the configuration mask.
    - Reset clears memory before the first recorded row and no environment/configuration/episode inherits prior state.
    - Mutating PWM, applied wrench, oracle or simulator actuator truth without changing causal history leaves primary memory bytes unchanged.
    - D physics substeps and one control-interval proxy update agree within `8*eps(float32)` for constant command.
  </behavior>
  <action>RED first with a fake runtime that exposes deliberate nonzero forbidden diagnostics and multiple episode/env resets. GREEN by implementing an additive `KoopmanBridgeV21` that owns one proxy per environment/episode, uses catalog control masks before `advance`, and builds rows only through the strict v2.1 builder. Record physics_dt_s, integer decimation and control_dt_s from runtime provenance and reject disagreement. The Bridge must not read PWM, wrench, oracle, future memory or simulator actuator truth to construct any primary input. Keep the legacy Bridge unchanged per D81-01..09 and D81-28.</action>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest -q tests/test_koopman_bridge_v21.py tests/test_koopman_bridge_v2.py tests/test_thruster_dynamics.py --basetemp .pytest-tmp\phase81-01-bridge</automated>
  </verify>
  <done>The Bridge proves causal row ordering, reset isolation, timing provenance and proxy/substep equivalence without changing any old v2 path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime command/time → actuator dynamics | Batched external tensors can corrupt state through wrong time, shape or tau. |
| Runtime telemetry → v2.1 transition | Simulator-only truth must not cross into primary model inputs. |
| JSONL/manifest → dataset | Untrusted or mislabeled versions can cause recurrence or evidence leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-81-01 | Tampering | `DynamicsFirstOrder.update` | mitigate | Validate monotonic end time, shape/device/dtype/finite values and commit clock only after a successful update. |
| T-81-02 | Information disclosure | v2.1 Bridge primary view | mitigate | Construct primary arrays only from state, proxy and masked virtual control; mutation-test PWM/wrench/oracle independence. |
| T-81-03 | Spoofing | schema version boundary | mitigate | Exact field/version validation; v2 and v2.1 reject each other's records. |
</threat_model>

<verification>
1. Every production change follows an observed targeted RED and a fresh GREEN run.
2. Run the three targeted commands above with repository-local basetemp.
3. Confirm old `schema_v2`, `dataset_v2`, `koopman_bridge_v2` source bytes are not modified by this plan.
4. Do not run any collector, bundle, SSH, server command or formal LOCO.
</verification>

<success_criteria>
- KIDR-01 closes the first-interval timing defect for shared simulation infrastructure.
- KIDR-02/03 provide a strict causal 19D local data path with one recurrence owner and old-v2 isolation.
- No artifact created here can be described as server, formal LOCO, D-23-approved or promotion evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-phase-8-1-local-simulator-and-koopman-identification-repair/08.1-01-SUMMARY.md` with RED/GREEN commands, changed files, timing/recurrence evidence, v2 compatibility results and deviations.
</output>
