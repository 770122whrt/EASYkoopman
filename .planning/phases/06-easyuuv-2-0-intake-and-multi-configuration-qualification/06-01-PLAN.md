---
phase: 06-easyuuv-2-0-intake-and-multi-configuration-qualification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - easyuuv_v2-main/
  - easyuuv_nc/
  - pyproject.toml
  - easyuuv_nc/package_paths.py
  - easyuuv_nc/env/assets/warpauv.py
  - easyuuv_nc/SNAPSHOT_PROVENANCE.md
  - tests/test_easyuuv_v2_package.py
autonomous: true
requirements: [QUAL-01, QUAL-03, QUAL-07]
decisions: [D-01, D-02, D-03, D-05, D-06, D-11, D-14]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: received snapshot is already an isolated earlier commit; package normalization is a later commit and does not require force-push"
    - "D-03: v1.0 tag, archived planning records and retained v1 phase directories remain untouched"
    - "D-05: import, editable install, gym registration and asset lookup use the canonical easyuuv_nc package name"
    - "D-06: all configurations resolve the one shared data/embodiment/embodiment.usd asset"
    - "D-11/D-14: local package probes do not claim Isaac physics evidence and do not change v1 Koopman dimensions"
  artifacts:
    - path: pyproject.toml
      provides: editable package discovery for easyuuv_nc and embodiment package data
      contains: 'name = "easyuuv-nc"'
    - path: easyuuv_nc/package_paths.py
      provides: package-root-confined absolute asset resolution
      contains: "def resolve_package_asset"
    - path: easyuuv_nc/SNAPSHOT_PROVENANCE.md
      provides: received-path and snapshot-commit provenance record
      contains: "received_path: easyuuv_v2-main"
    - path: tests/test_easyuuv_v2_package.py
      provides: Isaac-free package/import/asset/source-contract tests
      contains: "test_embodiment_usd_resolves_inside_package"
  key_links:
    - from: easyuuv_nc/env/assets/warpauv.py
      to: easyuuv_nc/package_paths.py
      via: resolve_package_asset("data/embodiment/embodiment.usd")
      pattern: "resolve_package_asset"
    - from: pyproject.toml
      to: easyuuv_nc/data/embodiment/embodiment.usd
      via: setuptools package-data declaration
      pattern: "data/embodiment/\\*\\.usd"
---

<objective>
在收到的源码快照已经独立提交的前提下，把物理目录、editable install、degraded import、gym registration 和共享 USD 路径统一到 `easyuuv_nc`，同时留下可审计的 snapshot provenance。

Purpose: 消除 Phase 6 的首要集成歧义——源码自称 `easyuuv_nc`，但目录/安装合同尚不存在。
Output: canonical package tree、`pyproject.toml`、安全 asset resolver、provenance record 和 Isaac-free package tests。
</objective>

<execution_context>
@.planning/phases/06-easyuuv-2-0-intake-and-multi-configuration-qualification/06-SPEC.md
@.planning/phases/06-easyuuv-2-0-intake-and-multi-configuration-qualification/06-CONTEXT.md
@.planning/phases/06-easyuuv-2-0-intake-and-multi-configuration-qualification/06-RESEARCH.md
@.planning/phases/06-easyuuv-2-0-intake-and-multi-configuration-qualification/06-PATTERNS.md
</execution_context>

<context>
@docs/Agentic_AUV_v2_milestone_design.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@easyuuv_v2-main/__init__.py
@easyuuv_v2-main/env/assets/warpauv.py
@easyuuv_v2-main/README.md
@koopman/model.py
@koopman/mpc.py
</context>

<threat_model>
  <assets>
    <asset>Received snapshot provenance and immutable v1.0 history</asset>
    <asset>Installed package root and shared USD asset path</asset>
    <asset>Developer/server Python environment used by editable install</asset>
  </assets>
  <trust_boundaries>
    <boundary>Untracked received directory to tracked Git snapshot</boundary>
    <boundary>User-supplied relative asset path to resolved local filesystem path</boundary>
    <boundary>Repository metadata to server editable-install environment</boundary>
  </trust_boundaries>
  <threats>
    <threat severity="HIGH">Package normalization is committed before or mixed into the received snapshot, destroying provenance.</threat>
    <threat severity="HIGH">A broad recursive move/delete targets the wrong directory or removes retained v1 phase evidence.</threat>
    <threat severity="MEDIUM">Asset resolution accepts traversal outside `easyuuv_nc` or depends on the process working directory.</threat>
    <threat severity="MEDIUM">Packaging accidentally includes `source/`, checkpoints, planning files or legacy root modules.</threat>
  </threats>
  <mitigations>
    <mitigation>Resolve and verify exact absolute source/destination paths before `git mv`; never use recursive delete or reset.</mitigation>
    <mitigation>Abort unless an earlier commit adds only `easyuuv_v2-main/` and no integration files.</mitigation>
    <mitigation>Constrain asset paths under `PACKAGE_ROOT` after `Path.resolve()` and require an existing file.</mitigation>
    <mitigation>Use setuptools include pattern `easyuuv_nc*` and explicit embodiment package-data only.</mitigation>
  </mitigations>
  <blocked_on severity="HIGH">Any unresolved HIGH threat blocks this plan; do not proceed with rename or commit.</blocked_on>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Verify snapshot boundary and normalize the tracked directory</name>
  <files>easyuuv_v2-main/, easyuuv_nc/, easyuuv_nc/SNAPSHOT_PROVENANCE.md</files>
  <read_first>
    <file>docs/Agentic_AUV_v2_milestone_design.md</file>
    <file>.planning/phases/06-easyuuv-2-0-intake-and-multi-configuration-qualification/06-CONTEXT.md</file>
    <file>easyuuv_v2-main/__init__.py</file>
    <file>.planning/milestones/v1.0-ROADMAP.md</file>
  </read_first>
  <action>
1. Resolve the repository root and both exact paths. Abort unless `easyuuv_v2-main` exists under the repository root, `easyuuv_nc` does not exist, and neither path is a symlink escaping the repository.
2. Find the earlier snapshot commit with `git log --diff-filter=A --format=%H -- easyuuv_v2-main`. Abort if no commit exists or if `git show --format= --name-only <hash>` contains a path outside `easyuuv_v2-main/`.
3. Run `git mv easyuuv_v2-main easyuuv_nc`; do not use `Move-Item`, recursive deletion, `git reset`, or force-push.
4. Create `easyuuv_nc/SNAPSHOT_PROVENANCE.md` with exact fields `received_path: easyuuv_v2-main`, `canonical_path: easyuuv_nc`, `snapshot_commit: <40-char hash>`, `snapshot_kind: received-unmodified`, and `integration_change: directory-normalization`.
5. Confirm `git diff --name-status` reports the tree as rename/add/delete metadata only and that `.planning/milestones/`, `.planning/reports/`, `source/` and root v1 Koopman files are absent from this task's diff.
  </action>
  <verify>
    <automated>git diff --check &amp;&amp; git diff --name-status -- easyuuv_nc easyuuv_v2-main</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`easyuuv_v2-main` no longer exists and `easyuuv_nc/__init__.py` exists.</criterion>
    <criterion>`easyuuv_nc/SNAPSHOT_PROVENANCE.md` contains `received_path: easyuuv_v2-main` and a 40-character `snapshot_commit`.</criterion>
    <criterion>The referenced snapshot commit contains no path outside `easyuuv_v2-main/`.</criterion>
    <criterion>`git diff -- .planning/milestones .planning/reports source koopman/model.py koopman/mpc.py` is empty for this task.</criterion>
    <criterion>D-01, D-02 and D-03 are satisfied without reset, force-push or deletion.</criterion>
  </acceptance_criteria>
  <done>The tracked tree is renamed only after an isolated snapshot commit is proven, and the provenance file records that commit.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical packaging and package-confined asset resolution</name>
  <files>pyproject.toml, easyuuv_nc/package_paths.py, easyuuv_nc/env/assets/warpauv.py</files>
  <read_first>
    <file>easyuuv_nc/__init__.py</file>
    <file>easyuuv_nc/env/__init__.py</file>
    <file>easyuuv_nc/env/assets/warpauv.py</file>
    <file>easyuuv_nc/data/embodiment/config.yaml</file>
  </read_first>
  <action>
1. Create repository-root `pyproject.toml` using `setuptools.build_meta`, project name `easyuuv-nc`, version `2.0.0`, `requires-python = ">=3.10"`, package discovery `include = ["easyuuv_nc*"]`, and package data `easyuuv_nc = ["data/embodiment/*.usd", "data/embodiment/*.yaml"]`. Do not declare or package `source/`, `.planning/`, checkpoints, artifacts or v1 root modules.
2. Create `easyuuv_nc/package_paths.py` with `PACKAGE_ROOT = Path(__file__).resolve().parent` and `resolve_package_asset(relative_path: str | Path) -> Path`. Resolve `PACKAGE_ROOT / relative_path`, reject absolute inputs, reject targets whose resolved path is neither `PACKAGE_ROOT` nor a descendant, and raise `FileNotFoundError` when the target is not a file.
3. Export `EMBODIMENT_USD_PATH = resolve_package_asset("data/embodiment/embodiment.usd")`.
4. Update `easyuuv_nc/env/assets/warpauv.py` to import `EMBODIMENT_USD_PATH` and pass `str(EMBODIMENT_USD_PATH)` to `UsdFileCfg`. Remove the `../../data` string and all dependence on current working directory.
5. Keep the four gym IDs and `_ENTRY = "easyuuv_nc.env:EasyUUVEnv"` unchanged in `easyuuv_nc/__init__.py`. Preserve its rule that only missing `gymnasium` or `omni*` modules are suppressed.
  </action>
  <verify>
    <automated>python -m pip install -e . --no-deps &amp;&amp; python -c "import easyuuv_nc; from easyuuv_nc.package_paths import EMBODIMENT_USD_PATH; assert EMBODIMENT_USD_PATH.is_file(); print(EMBODIMENT_USD_PATH)"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`pyproject.toml` contains `name = "easyuuv-nc"`, `include = ["easyuuv_nc*"]`, and `data/embodiment/*.usd`.</criterion>
    <criterion>`easyuuv_nc/package_paths.py` contains `def resolve_package_asset` and rejects `../` traversal after resolution.</criterion>
    <criterion>`easyuuv_nc/env/assets/warpauv.py` contains `EMBODIMENT_USD_PATH` and no longer contains `../../data/embodiment`.</criterion>
    <criterion>Editable install and the Isaac-free import/asset probe exit 0 from the repository root and from a different current working directory.</criterion>
    <criterion>D-05 and D-06 are satisfied without an import alias, symlink or duplicate source tree.</criterion>
  </acceptance_criteria>
  <done>`easyuuv_nc` installs/imports from one canonical path and resolves the shared USD inside the package.</done>
</task>

<task type="auto">
  <name>Task 3: Lock package behavior with Isaac-free tests and v1 isolation checks</name>
  <files>tests/test_easyuuv_v2_package.py</files>
  <read_first>
    <file>tests/test_isaaclab2_source_contract.py</file>
    <file>tests/test_phase1_source_contract.py</file>
    <file>easyuuv_nc/__init__.py</file>
    <file>easyuuv_nc/package_paths.py</file>
    <file>koopman/model.py</file>
    <file>koopman/mpc.py</file>
  </read_first>
  <action>
Create `tests/test_easyuuv_v2_package.py` with behavior-level tests that:
- assert `SUPPORTED` package discovery does not include directories outside `easyuuv_nc` by inspecting `pyproject.toml`;
- call `resolve_package_asset("data/embodiment/embodiment.usd")` and assert the file is inside `PACKAGE_ROOT`;
- assert traversal `resolve_package_asset("../koopman/model.py")` raises `ValueError`;
- run a subprocess import from a temporary current working directory with repository root on `PYTHONPATH` and assert `easyuuv_nc.__file__` resolves under the canonical directory;
- inspect `easyuuv_nc/__init__.py` for exactly four task IDs and `_ENTRY = "easyuuv_nc.env:EasyUUVEnv"`;
- assert `koopman/model.py` still imports `PWM_DIM` and defaults `control_dim: int = PWM_DIM`, while `koopman/mpc.py` still bounds PWM in `[-1,1]`.

Do not import Isaac-dependent `EasyUUVEnvCfg` in this test file.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_easyuuv_v2_package.py &amp;&amp; python -m compileall easyuuv_nc tests/test_easyuuv_v2_package.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`tests/test_easyuuv_v2_package.py` contains `test_embodiment_usd_resolves_inside_package` and `test_asset_resolver_rejects_parent_traversal`.</criterion>
    <criterion>The targeted pytest command exits 0 without importing `omni` or starting Isaac.</criterion>
    <criterion>The subprocess import succeeds outside the repository current working directory.</criterion>
    <criterion>Tests assert four gym IDs and the unchanged v1 `PWM_DIM` model/MPC contract.</criterion>
    <criterion>D-11 and D-14 are explicitly enforced by executable tests.</criterion>
  </acceptance_criteria>
  <done>Package/import/asset tests pass without Isaac and the v1 8D PWM contract remains unchanged.</done>
</task>

</tasks>

<verification>
1. `git diff --check` exits 0.
2. `python -m pip install -e . --no-deps` exits 0 without network dependency resolution.
3. The Isaac-free package/asset subprocess probe exits 0 from a temporary current working directory.
4. `python -m pytest -q tests/test_easyuuv_v2_package.py` exits 0.
5. `git diff v1.0 -- .planning/milestones .planning/reports koopman/model.py koopman/mpc.py` shows no Phase 6 modification to frozen archives or v1 control semantics.
6. The plan executor records the verified snapshot commit in `06-01-SUMMARY.md`.
</verification>

<success_criteria>
- Snapshot provenance is independently verifiable and package normalization occurs only afterward.
- `easyuuv_nc` is editable-installable and importable with a single package identity.
- Shared USD resolution is absolute, package-confined and current-working-directory independent.
- Package tests are Isaac-free and preserve the v1 8D PWM contract.
- No HIGH threat remains unresolved.
</success_criteria>

<output>
After completion, create `.planning/phases/06-easyuuv-2-0-intake-and-multi-configuration-qualification/06-01-SUMMARY.md` with task commits, snapshot hash, package probe outputs, deviations, threat-model disposition and self-check result.
</output>
